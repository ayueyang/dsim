# 2026-09-19 修复执行记录：阶段 0

基线：main@1816213；仅执行修复计划阶段 0。计划与审查文档只读。

## 用户确认的决策

- T0.1 保持 minSdk 24，仅替换 Base64，不改 PBKDF2。
- T0.2/T0.3 的具体要求优先于 AGENTS C22/C24 的旧表述：手动 PUBACK 不变、取消仍不 ack，基础设施失败不 ack，协议毒消息 ack。
- HISTORY_QUEUE_BATCH 保持 10 分钟窗口。未分类异常保守视为 RetryableFailure，不 ack。
- JVM Base64 空桩导致失败时，允许仅 src/test 的适配，不增加依赖，不以它证明 Android API 兼容性。
- 每任务独立 conventional commit；不采用阶段 0 标题下“一起提交”的建议。

## T0.1 Base64 平台兼容

变更：MqttProtocol.kt 改用 android.util.Base64，URL_SAFE | NO_WRAP | NO_PADDING；保持 12 字节安全随机 nonce。同步修正该处测试能力注释。
测试：src/test/java/android/util/Base64.java 提供仅 JVM 的编码/解码适配；nonce 测试收紧为 16 位 URL-safe 无填充字符。

执行（PowerShell；所有 Gradle 命令均设置 JAVA_HOME=C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot，并传相同 org.gradle.java.installations.paths）：

1. `./gradlew.bat :app:compileDebugKotlin :app:lintDebug :app:cleanTestDebugUnitTest :app:testDebugUnitTest --continue`：退出 1；`94 tests completed, 9 failed`，9 个 codec 编码测试 NullPointerException（Android 空桩）；lint 为 `8 errors, 313 warnings`。
2. 增加测试适配后：`./gradlew.bat :app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest`：退出 0，`BUILD SUCCESSFUL in 41s`。
3. 解析本次 lint XML：`NewApi total: 3`；`MqttProtocol NewApi: 0`。T0.1 的 lint 定点验收通过，**全项目 lint 未通过**。API 24/25 配置保存+发送未执行，不能声称通过。
4. 源文件编码初检：`UTF8_OK BOM=False CRLF=0`；`git diff --check` 无输出、退出 0。

## 新发现，只记录不修改

历史记录（T0.1 修复后、F1 修复前）：lint 实测 8 errors/313 warnings，MqttProtocol NewApi 已归零。未在 1816213 上测过修复前基线 lint，不能称“全部存量/与阶段0前一致”，也不把推算的9 errors当实测。该8项如下（HardwareProbeUtils 三项本轮已由 F1 修复）：
- OnboardingActivity.kt:157 MissingSuperCall。
- HardwareProbeUtils.kt:238 API 26 getSimState；:293 API 29 getSubscriptionIds；:294 API 29 isValidSubscriptionId。
- AndroidManifest.xml:13–16 四项 PermissionImpliesUnsupportedChromeOsHardware。

## T0.2 按消息语义拆分防重放时间窗

变更：ReplayGuard.Policy 穷举映射所有入站类型。SMS_SYNC / SEND_CMD_RESULT / HISTORY_SYNC_ACK 无时间窗；SEND_CMD / PING / PONG / HISTORY_QUEUE_BATCH 保持 10 分钟；OFFLINE 保持 24 小时。nonce 缓存仅按容量淘汰，不因短窗消息清除旧的持久消息 nonce；无 MQTT dup 绕过。补充 LWT 超过 24 小时的注释。

验证：
- `./gradlew.bat :app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest`：退出 0，`BUILD SUCCESSFUL in 1m 24s`；XML 汇总 `tests= 96 failures= 0 errors= 0`。
- `adb -s emulator-5554 shell am instrument -w -e class com.example.dsim.ReplayDeliveryTest com.example.dsim.test/androidx.test.runner.AndroidJUnitRunner`：`OK (1 test)`，退出 0。使用真实 Android Base64 + 内存 Room，30 分钟前信封经 guard 接受并插入数据库。
- 新 JVM 用例验证所有消息类型策略、过期 SEND_CMD 不进入执行分支、长期 nonce 不被短窗流量清除。
- 真实双模拟器离线 30 分钟补投仍待阶段联调验收；上述固定时间戳仪器用例不能替代该 E2E。

## T0.3 入站三态与提交后 nonce

变更：
- 新增 InboundOutcome：Committed / PermanentlyRejected / RetryableFailure。
- InboundDispatcher 仅对前两态 ack；SQLite/IO 及未分类异常不 ack；取消继续抛出；自身回声仍直接 ack；dup 只记日志。
- ReplayGuard 拆为无副作用 checkFreshness 与 markConsumed。InboundCommitGate 用 Mutex 串行包围检查、挂起处理及提交后标记；失败、永久拒绝、取消均不污染 nonce。此锁防止拆成两步后并发同 nonce 同时通过。
- MqttInboundHandler 返回三态，解密失败/坏 JSON/未知 action 永久拒绝；基础设施异常上交 dispatcher，不再吞成正常返回。
- **追加范围已由用户裁决允许**：OutgoingSmsDispatcher 既有 5 处 require 和额度拒绝仅改为 SendCommandRejectedException；通过 prepareSendCommand 保留原有业务失败回执。额度算法、认领顺序、事务边界、运营商调用均不变。普通 IllegalArgumentException/IllegalStateException 仍是未知错误，不 ack。

验证：
- 最初三态改造：`BUILD SUCCESSFUL in 53s`，`tests= 106 failures= 0 errors= 0`；新增业务拒绝区分后再次构建 + clean 单测 + 两个 APK：`BUILD SUCCESSFUL in 44s`，`tests= 109 failures= 0 errors= 0`。
- `adb -s emulator-5554 shell am instrument -w com.example.dsim.test/androidx.test.runner.AndroidJUnitRunner`：退出 0，`OK (13 tests)`。包括 30 分钟前信封落入真实 Room，以及注入 SQLiteException 导致 Room 事务回滚/无 ack/nonce 未消耗，同一 gate 重投后提交+ack。
- 新增 JVM 验证：SQLite/IO/未知异常不 ack；JsonSyntaxException ack；三态分支；取消不 ack；同实例同 nonce 失败重投；并发同 nonce 仅处理一次；业务拒绝失败回执与回执写入失败可重试。
- 全部变更文件检查：UTF-8 无 BOM、无 CR；`git diff --check` 退出 0。

## 阶段全局回归（当前已执行部分）

按计划原命令经 Git Bash 执行：

```bash
export JAVA_HOME="/c/Program Files/Microsoft/jdk-21.0.7.6-hotspot"
./gradlew :app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest \
  -Dorg.gradle.java.installations.paths="C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot"
```

实际输出：`BUILD SUCCESSFUL in 25s`；退出 0。
`bash scripts/verify-dsim.sh emulator-5554 emulator-5556`：`PASS=12 FAIL=0`；退出 0（仅将 VDSIM_PYTHON 指向现存 C:/Python314/python.exe，不改脚本）。
设备为 API 36 双模拟器；B 经真实设置 UI 将测试 broker 改为本机 tcp://10.0.2.2:1883，与 A 一致。B 初始旧 APK 的 dataSync 开机崩溃在覆盖安装本轮 APK 后不再阻塞测试；未为此改生产代码。

风险边界（F2更正）：未知异常/基础设施故障重投无次数上限，长期不ack的消息会耗尽broker该持久会话的入站在途窗口，随后新消息投递整体停摆；下个会话重投若仍失败，同样会再次停摆，重连不保证自愈。按用户裁决优先不丢数据，本轮仅把这一已知、有意的可用性取舍写入AGENTS/FIXES；未实现有界隔离/告警，不新增主动断线重连机制。不得仅达到次数阈值就丢弃并ack；持久隔离/恢复方案必须另行请用户批准。完整协议字段校验仍属后续阶段。
REFACTORING.md 没有与 T0.1–T0.3 一一对应的待勾销 W 项，未将 W18 等较大工作项误标完成。

## F1 审查收尾：HardwareProbeUtils SDK 守卫

- 文件：HardwareProbeUtils.kt。getSimState(slotIndex) 仅 API26+ 调用；API24/25 的 slot0 使用 simState，其余 slot 返回 UNKNOWN。getSubscriptionIds/isValidSubscriptionId 仅 API29+ 调用，较低版本继续既有 getActiveSubscriptionInfoForSimSlotIndex/activeList fallback。不扩大 catch 到 Throwable、不抑制 NewApi。
- 修复前（fc3fef2）：Git Bash + JDK21 + installations.paths，`:app:lintDebug` → `BUILD FAILED in 32s`、exit1、8 errors/313 warnings；XML NewApi=3（HardwareProbeUtils:238/293/294），MqttProtocol=0。分析任务 UP-TO-DATE；这是 F1 前状态，不是 1816213 基线。
- 修复后：相同环境 `:app:compileDebugKotlin :app:lintDebug` → compileDebugKotlin 完成；lint `BUILD FAILED in 2m 30s`、exit1、5 errors/313 warnings。XML NewApi=0（包括 HardwareProbeUtils/MqttProtocol）。剩余 MissingSuperCall×1、ChromeOS硬件权限声明×4 未改。
- `git diff --check` exit0；源文件 BOM=False/CR=0。F1 定向 lint 验收通过；全量 lint 仍失败，低 API 运行时仍未验证，不以 lint 冒充。

## F3：400 条突发吞吐与进程死亡恢复

文件：新增 scripts/measure-inbound-burst.py；只增加测试探针，不改生产同步/锁语义。沿用 batch I 的 DSM3 + 400 个唯一 UUID + QoS1 silent SmsSync，独立 witness 必须收到400条，Room总行数/唯一UUID都须400。口令走 DSIM_TEST_PASSWORD 环境变量，不入文件。

构建命令均为 Git Bash + JAVA_HOME=/c/Program Files/Microsoft/jdk-21.0.7.6-hotspot + `-Dorg.gradle.java.installations.paths="C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot"`：`:app:assembleDebug` 当前 c2bddfb BUILD SUCCESSFUL in50s；独立 detached worktree 1816213（C:/Users/admin/AgentDock/dsim-baseline-1816213，另设ANDROID_HOME）BUILD SUCCESSFUL in2m22s；均exit0，基线worktree status为空。

实跑入口：`C:/Python314/python.exe C:/Users/admin/AgentDock/dsim-f3-suite.py`（最终session-c6aa128a88bc898008cc51a2，exit0）。每轮实际命令：`python scripts/measure-inbound-burst.py --label <1816213|c2bddfb> --mode <nokill|kill> --serial emulator-5556 --adb C:/platform-tools/adb.exe --topic dsim/test/9f3a2b --evidence C:/Users/admin/AgentDock/dsim-f3-evidence`。两版同一B(API36)/同本机Mosquitto，无发布间隔；每轮重启订阅后预热3秒；install -r保留数据，finally输出CURRENT_APK_RESTORED。

| 版本/模式 | 全部400条入库耗时上界 | dup=true次数 | 唯一行/witness | 实际force-stop完成时刻 |
|---|---:|---:|---|---|
| 1816213 / nokill | 10.054s | 0 | 400/400；400/400 | — |
| c2bddfb / nokill | 14.667s | 0 | 400/400；400/400 | — |
| 1816213 / kill | 23.831s | 20 | 400/400；400/400 | +1.319s |
| c2bddfb / kill | 27.540s | 20 | 400/400；400/400 | +1.236s |

口径：首条publish前monotonic到首次**一致快照**读到400条，包含adb/采样时间；kill模式包含停机与重启。为防DB/WAL采样竞态，读取主DB→WAL→主DB，仅在两次主DB字节相同时采信；忙写丢弃样本，不计为0；总截止300秒。它不是纯handler计时。单组固定顺序、保留DB且宿主负载未隔离，不能作统计结论。

最终证据子目录：F3-1816213-nokill-ed63adf5、F3-1816213-kill-fe72e357、F3-c2bddfb-nokill-8532724e、F3-c2bddfb-kill-b8cc7a2b（各含result.json/trace.json/logcat.txt/数据库快照；含设备数据，未入git）。APK SHA256：基线 f8387bde13823d91cdd32c95ad8cdfb0bec2034cad62e798a57edc5a4ae4bb8c；当前 131374d935d5ec6ace414091f67acb26505f8d400c7eaba40f9d0df3194719c7。

结论：本轮两版均0丢失/0重复行；无kill耗时上界增加45.9%，kill增加15.6%，重投次数未增。**存在吞吐退化信号，不能宣称无退化**；建议后续先多轮交错测量/剖析，再评估按sender+nonce预留-提交/限并发设计；未获授权不修改全局Mutex。

失败与偏离：A在线但已无配置/无MQTT服务，首轮订阅前超时，未发消息；随后统一用B。B首组虽齐全，但中途计数121→103暴露活跃快照竞态，其时间作废；夹读方案首次因5次忙写直接失败，改为跳过无效样本直到统一截止后全组重跑。原始失败/作废证据保留。Python语法编译与git diff --check通过。

## F6：账目与显式分类小修

- InboundOutcome.kt 显式 `is SQLiteException, is IOException -> RetryableFailure`，取消优先抛出、JsonSyntaxException永久拒绝、未知异常重投的语义不变。
- InboundDispatcherTest、InboundCommitGateTest、ReplayGuardTest、SendCommandPreparationTest、ReplayDeliveryTest（本阶段触及的5份测试）改显式导入；不全仓清理历史通配。定向git grep通配匹配=0。
- FIXES及本计划progress/findings明确：8 errors/313 warnings是T0.1后/F1前实测，未测1816213修复前lint；不再称全部存量/与阶段0前一致。
- Git Bash + 固定JDK21/安装路径参数：`:app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` exit0（session-704847f9434a24b644c00e22）；XML `tests=109 failures=0 errors=0`。该次输出尾部被工具4000字节限制截断，未记录BUILD耗时。
- 两APK `adb -s emulator-5554 install -r` 均Success；`adb -s emulator-5554 shell am instrument -w com.example.dsim.test/androidx.test.runner.AndroidJUnitRunner` → `Time:10.577`、`OK (13 tests)`、exit0（session-48eaf31ce74d4acd873c9425）。
- 六个变更源文件逐字节UTF8_OK/BOM=False/CR=0；git diff --check exit0。验收通过。

## F2：仅记录已知停摆取舍

修改AGENTS C24的过期“所有其他异常都ack”表述为已裁决三态，并新增无界重投风险说明；修正本记录中“直到下个会话”的误导。无产品代码/DB/重试策略改动。核对 `git diff -- AGENTS.md FIXES_2026-09-19.md`、`git diff --check`；仅文档变更，diff检查exit0。有界隔离仍未实现，必须另请批准。

## F4：过期 SEND_CMD 的安全收敛方案（仅提案，未实现）

### 目标
`ReplayGuard` 发现过期 `SEND_CMD` 后仍应拒绝执行；但拒绝不能让请求端的 `sms_messages.status=0` 永久停在“发送中”。提案是由执行端持久发一条 `SEND_CMD_RESULT(success=false, state=FAILED, message=已过期未执行)`，让既有请求端 `handleSendCommandResult` 将状态收敛为失败。不要放宽 SEND_CMD 的10分钟窗口。

### 前置判断（缺一不可）
1. 只针对解密、解码成功且 `inbound is SendCmd` 的 `STALE` 拒绝；MISSING、DUPLICATE、非 SEND_CMD 的拒绝不生成业务失败回执。
2. `uuid` 与 `SendCmd.deviceId` 必须非空；优先要求 payload 的 `deviceId` 与 topic sender 一致，防止把失败回执路由给被伪造的设备。目标设备只取经校验的 requester deviceId。
3. 执行端先查 `dao.getSendCommand(uuid)`；**仅为 null 才允许生成过期回执**。已有 PENDING/UNKNOWN/SENT/FAILED 账本时只按已有账本语义处理，绝不能用“过期”结果覆盖真实执行结论。
4. 必须确认 `UsageModeManager.canUseCloud(context)`、`session.isConfigured` 与当前组配置可用。隐私本地模式继续遵守现有“确认后丢弃”语义，不为生成回执而新增云端上传点。
5. 失败回执先进入现有 `SyncOutbox`，而不是直发；使用已有唯一 `controlKey`，建议 `KIND_SEND_CMD_RESULT + uuid + "expired"` discriminator，避免重复拒绝创建多条相同 outbox。只有入队事务成功后才允许该 MQTT 报文 PUBACK。

### 实现边界
- 将 `InboundCommitGate` 的拒绝回调改为可挂起且可返回 outcome，或增加等价的“拒绝副作用已提交”路径：outbox 入队成功→`PermanentlyRejected`→ack；SQLite/IO 失败→`RetryableFailure`→不 ack、重投重试。不能继续用当前无返回值的 `onRejected` 在入队前 ack。
- 扩展现有 `MqttPublisher.publishSendCommandResult` 的参数/专用方法，使 `state=FAILED` 与 `expired` discriminator 显式编码；不新增实体字段、不递增 Room version、不改变 DSM3 或 MQTT topic 层级。
- 既有请求端已按 `state` 映射失败状态，需保留 `targetDeviceId` 校验；若新增稳定错误码，应先确认协议兼容范围，最小方案只复用现有 `message` 字段。

### 验收与回滚
- JVM：过期 SEND_CMD 不调用 dispatcher；无账本时生成一条可解密的 FAILED 回执，重复相同拒绝因 outbox key 幂等；已有账本时零过期回执；入队 SQLite 失败时无 ack、nonce 未消费，重投后可成功入队。
- 仪器/双机：请求端原 status=0 在收到回执后变 -1；执行端无 `send_commands` 执行记录、无运营商调用；断网时回执留在 `sync_outbox`，恢复后只发布一次。
- 回滚风险：错误的 deviceId/uuid 校验会把别人的气泡标成失败；错误的 ack 时序会丢掉“失败回执”或使过期报文反复重投；复用 `prepare_failed` discriminator 会覆盖/合并不同结论。故必须先加上述守卫与故障注入测试，再改生产路径。方案不应引入 schema migration，便于单提交回滚；回滚后已入 outbox 的过期回执仍可能发布，若要完全撤回需明确 outbox 数据清理策略，不能静默删除。

### 当前裁决状态
本轮只记录方案，未改代码、未改协议、未改数据库、未实现过期回执。需要用户批准前置判断和“本地模式不发回执”的语义后，才进入实现；不随本轮进入阶段 1。

## 运行时验证偏差（本轮收尾）

- API24/25/28：尝试用本机 SDK Manager 安装 `platforms;android-28` 与 `system-images;android-28;google_apis;x86`。下载过程实际报 `Error reading Zip content from a SeekableByteChannel`；清理后检查 `system-images/android-28/google_apis/x86/system.img=False`，只剩已删除的 `.installer` 临时目录；可用 AVD 仍只有 `Medium_Phone_API_36.1`、`dSIM_B`、`dSIM_C`。没有创建/启动 API28 模拟器，所以 T0.1/F1 的低 API 运行时路径**仍未验证**，未用 lint 或 API36 结果冒充。
- 同进程断网/重连脚本未执行：当前 A（emulator-5554）被覆盖安装后处于首次引导、无 `dSIM_UI_PREFS.xml`、无 MQTT 服务，且曾出现 System UI 无响应弹窗；只有 B 保持已配置。为避免绕过真实引导或把不满足前置条件的失败伪装成产品结论，本轮不跑该脚本，列为未验证。
- F3 最终 suite 的 B 网络设置由脚本 finally 恢复；当前 APK 已恢复并启动。

## T0.4：远端备份与分叉处理

- 第一次直接执行 `git push origin main` 被远端拒绝：`[rejected] main -> main (fetch first)`；不是强推。`git fetch origin main` 实测发现远端新增 `8cdffee Delete dSIM_技术文档_架构师版.md` 与 `688645b Delete 提示词`，均为远端已有历史，未覆盖本轮阶段0代码/FIXES/plan提交。
- 用 `git merge --no-ff --no-edit origin/main -m "chore(sync): integrate remote main before stage-zero backup"` 保留双方历史，生成 `931196e`；工作区仅保留用户/其他会话已有脏项与未跟踪只读文件，未加入提交。
- 再次 `git push origin main`：`688645b..931196e main -> main`，成功。随后 `git branch -vv`：`main 931196e [origin/main]`；`git rev-list --left-right --count origin/main...main`：`0 0`。未使用 force push。

## 阶段状态

T0.1/T0.2/T0.3及F1/F3/F6/F2完成；F4仅提案未实现。F3四轮400条均齐全；API24/25/28运行时与同进程断网/重连仍未验证，已记录原因。T0.4 push待本次账本提交后执行。没有进入阶段1。

## 收尾续作：仪器测试复跑（2026-09-19 晚，对应审查遗留项 §4.1）

命令（Git Bash + JDK 21，与阶段 0 一致）：`./gradlew :app:connectedDebugAndroidTest -Dorg.gradle.java.installations.paths="C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot" --console=plain`

设备：emulator-5554 = dSIM_B，API 36（Android 16），**冷启动**（`-no-snapshot` / `-gpu swiftshader_indirect` / `-feature -Vulkan` / `-icc-profile test-fixtures/icc/sim_a.xml`）。

原始输出（控制台）：

```
Starting 13 tests on dSIM_B(AVD) - 16
dSIM_B(AVD) - 16 Tests 13/13 completed. (0 skipped) (0 failed)
Finished 13 tests on dSIM_B(AVD) - 16
BUILD SUCCESSFUL in 3m 38s
```

机器可读判定：`app/build/outputs/androidTest-results/connected/debug/dSIM_B(AVD) - 16/test-result.textproto` → `scheduled_test_case_count: 13`、`test_status: PASSED`，13 个 `test_result` 全为 `PASSED`。用例分布：ExampleInstrumentedTest 1 / ReplayDeliveryTest 2 / SendCommandLedgerTest 5 / SyncOutboxDaoTest 5。**13/13 复现成功**；仍不覆盖 F1 改动的 `HardwareProbeUtils`（结论以 JVM 109/109 与 lint NewApi=0 为准）。

上次同一 HEAD 失败（`INSTRUMENTATION_FAILED: ... failed to attach`，XML `tests="0"`）的现场证据：故障时那台 5554 已连续运行约 15 小时，`adb logcat -b crash` 有多条 `DeadSystemException: The system died`，`adb shell pm list packages` 返回 `Failure calling service package: Broken pipe (32)`。处理：`adb -s emulator-5554 emu kill` → 清理 `dSIM_B.avd/multiinstance.lock` → 冷启动（`scripts/emu-up.sh` 的同一组参数，脚本未改）→ 同 HEAD 通过。判为**模拟器实例状态问题，非代码问题**；未回溯 attach 失败时刻日志（UTP 已停止 logcat 流）。

证据目录（仓库外，未入 git）：`C:\Users\admin\AgentDock\dsim-instrumented-evidence\run-20260919T1257Z-refresh\`（35 个文件：逐用例 logcat、test-result.textproto、utp.0.log、HTML 报告、README.md）；审查方上次失败产物另存于同级的 `run-20260919-reviewer-failed\`。

本轮未跑双机：这 13 个用例只覆盖 Room 与入站提交契约，不依赖 modem 或第二台设备，故 emulator-5556 未启动。

## 阶段 1 · T1.1 MQTT 阻塞调用收口（P1·S）

变更文件：`MqttSyncService.kt`、`MqttPublisher.kt`、`SettingsActivity.kt`（1 行，见 D1）、`UsageModeManager.kt`（1 行，见 D1）；新增 `app/src/androidTest/java/com/example/dsim/MqttLifecycleResponsivenessTest.kt`。

关键改动：

1. `client.timeToWait = 15_000L`（与 `connectionTimeout = 15` 对齐）。Paho 默认 `-1` = 无限等待，是三处阻塞可能变成无限挂起的根因之一。
2. 新增 `enqueueClientTeardown(cancelScopeAfter)`：`APPLY_LOCAL_MODE` / `ACTION_DISCONNECT` / `onDestroy` 不再在主线程直接调 `publishOfflineBestEffort()` / `disconnect()` / `close()`，而是立即摘除引用（`globalMqttClient = null`、`staticConfig = null`、`connectionStateFlow = false`）后向 `serviceScope` 提交串行动作：`previous?.join()` → `connectMutex.withLock { setCallback(null) → publishOfflineBestEffort(retired, retiredConfig) → disconnectForcibly(1_000, 1_000) → close(true) }`。
3. `onDestroy` 主线程等待 0 ms（不做 join），改由 teardown 动作的 `finally` 取消 `serviceScope`；满足"需等待时用有界超时（≤3 s）而非无限 join"的下界。
4. `publishOfflineBestEffort(client, config)` 改签名：原实现读 live `session` 与 `client()`，摘除后会拿到 null 或已变更的新组配置；topic 仍由 `CloudTopics.publishTopic(config.topic, deviceId)` 计算（C13 不变）。
5. `connectMutex` 与 `clientTeardownJob` 上移到 companion：客户端是进程级资源，服务重建时新旧实例的清理与连接必须共用同一把锁（C12 同 clientId + 同持久化目录）。`connectAndSubscribe()` 先 `clientTeardownJob?.join()` 再进锁；实际连接体拆为 `connectUnderLock()`，避免整段缩进变更。
6. 新增 `destroying` 标志：destroy 之后 `connectionLost` / `connectComplete` / `messageArrived` 不再回写状态或进入入站分发；`connect()` 阻塞期间被摘除的客户端，处置权归已排队的 teardown，不在 connect 返回后清理（否则会误清服务新实例的 `globalMqttClient`）。
7. 断开路径由 graceful `disconnect()` 改为 `disconnectForcibly(1_000, 1_000)`：先 QoS1 发布 OFFLINE，再给 1 s 静默期让在途报文完成、1 s 发送超时后强制关闭。理由：`disconnect()` 受 `timeToWait` 约束最长 15 s，弱网无 PUBACK 时必然吃满；OFFLINE 未获 PUBACK 时由 LWT / 下一个 PONG 纠正对端状态（与 AGENTS C14 及既有 24 h OFFLINE 窗口注释语义一致）。

偏差与待裁决：

- **D1（范围外 1 行 ×2）**：teardown 立即摘除客户端后，`hasClient()`（原语义"本进程已创建过客户端，无论是否连接"）在手动断开后变为 false，会让 `SettingsActivity.refreshPrivacySensitiveNotifications` 与 `UsageModeManager.applyMode(LOCAL_ONLY)` 跳过对**仍在前台运行**的服务的通知刷新 / 本地模式切换（用户可见后果：通知文案停留在"已手动断开"）。已将该谓词改名为 `isDaemonActive() = globalMqttClient != null || serviceAlive`（`serviceAlive` 在 `onCreate`/`onDestroy` 维护），两个调用点同步改名，调用点逻辑未变。若审查方要求保留原命名，可只回退命名。
- **D2**：teardown 未做主线程有界等待（0 ms 而非 ≤3 s）。代价：进程在 `onDestroy` 后立即被杀时，best-effort OFFLINE 可能来不及发出；原同步实现同样只在主线程存活期间有机会发出，而现在不再以主线程阻塞为代价，非正常断链由 LWT 覆盖。
- 未改动：`InboundCommitGate` 的 Mutex（W22 裁决）、6 个 `publishToGroup` 调用点、协议 / Room schema / 清单。

验证（Git Bash + JDK 21 + `installations.paths`，全部为真实输出）：

- 构建与单测：`./gradlew :app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` → `BUILD SUCCESSFUL in 1m 40s`、exit 0；XML 汇总 `JVM tests=109 failures=0 errors=0`。
- 弱网验收（新仪器用例，emulator-5554 = dSIM_B / API 36）：用例在设备内起 `127.0.0.1` 环回 MQTT 测试 Broker，完成 CONNACK/SUBACK 并正常回 PUBACK 至订阅稳定后，**丢弃全部后续 QoS1 PUBACK**（等价弱网 / broker 无响应），依次执行手动断开、切本地模式、杀服务三种动作，同时用 `Choreographer` 连续计帧。结果 `OK (1 test)`、`Time: 35.225`；logcat：

  ```
  action=com.example.dsim.DISCONNECT          frames=331 maxMainGapMs=28
  action=com.example.dsim.APPLY_LOCAL_MODE    frames=329 maxMainGapMs=46
  action=destroy                              frames=330 maxMainGapMs=25
  ```

  三种动作主线程最大帧间隔 25–46 ms（阈值 5 s）。每轮先断言"黑洞 Broker 确实收到可解密的 OFFLINE 报文"，证明 OFFLINE 发布仍真实发生，只是移出主线程。用例前置断言 `sync_outbox` 为空（不把用户待发数据卷入合成组），运行期临时切 `RECEIVE_ONLY`，`finally` 还原原 `USAGE_MODE` 字节；不修改已保存的 broker/topic/口令，不清库。
- 编码与空白：5 个变更文件 `BOM False / CR 0 / UTF8 True`；`git diff --check` 无输出、exit 0。
- 未做（不以其它证据冒充）：iptables 丢包与"直接关停 broker"的独立测量、真实公网 broker、真机（非模拟器）；`scripts/verify-dsim.sh` 12/12 双机回归按计划在阶段末统一执行。

## 阶段 1 · T1.2 radarEventFlow 缓冲化 + 生命周期感知收集（P1·S）

变更文件：`MqttSyncService.kt`（声明）、`MqttInboundHandler.kt`（emit → tryEmit）、`DeviceManagerActivity.kt`、`SettingsActivity.kt`（两个收集端）；新增 `app/src/androidTest/java/com/example/dsim/RadarFlowLifecycleTest.kt`。

关键改动：

1. `radarEventFlow` 由 `MutableSharedFlow<String>()` 改为 `MutableSharedFlow<String>(extraBufferCapacity = 64, onBufferOverflow = BufferOverflow.DROP_OLDEST)`，并在声明处写明原因：radar 事件只是 UI 提示，收集端停滞时绝不能挂起发射方——发射点位于 `InboundCommitGate` 的 Mutex 临界区内（W22）。
2. `MqttInboundHandler` 的 Pong 分支 `emit(decryptedJson)` → `tryEmit(decryptedJson)`（返回值有意忽略：DROP_OLDEST 下恒为 true，丢弃的是最旧的 UI 提示）。
3. 两个收集端改为 `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { radarEventFlow.collect { … } } }`，收集体逻辑一字未改；两个 Activity 各补 `androidx.lifecycle.Lifecycle` 与 `androidx.lifecycle.repeatOnLifecycle` 导入。
4. 未改动同一文件内其它 `lifecycleScope.launch { … collect }`（`connectionStateFlow`、`SystemHistoryImportService.stateFlow`）：不在本任务范围，且它们是 StateFlow，不存在挂起发射方的问题。

验证（Git Bash + JDK 21，全部真实输出）：

- 构建与单测：`./gradlew :app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` → `BUILD SUCCESSFUL in 1m 32s`、exit 0；`JVM tests=109 failures=0 errors=0`。
- 新仪器用例 `RadarFlowLifecycleTest`（emulator-5554 = dSIM_B / API 36）→ `OK (3 tests)`、`Time: 83.476`。三个用例以生产代码可观测的 `radarEventFlow.subscriptionCount` 为断言面（不是自建 flow）：
  1. `backgroundedActivityStopsCollectingAndResumesOnReturn`：DeviceManagerActivity RESUMED → 订阅数 1；`moveToState(CREATED)`（退后台）→ 0；回到 RESUMED → 1；scenario 关闭后 → 0（无泄漏收集器）。
  2. `onlyTheVisibleActivityOfTwoStackedOnesCollects`：DeviceManager 之上再启动 SettingsActivity（两 Activity 同栈）→ 订阅数仍为 1（只有 STARTED 的那个在收集，被压栈者已停止）；两者都退后台 → 0。
  3. `busyMainThreadDoesNotSuspendTheEmitter`：SettingsActivity RESUMED 且收集器活跃时，用 `runOnMainSync { Thread.sleep(3000) }` 人为占住主线程，随后连发 200 个事件（远超 64 缓冲）：200 次 `tryEmit` 全部返回 true，总耗时 < 200 ms（实测断言通过），主线程解除阻塞后收集器仍存活（订阅数 1）。这正是 Pong 处理协程在 UI 卡顿时不再挂起的时序证据。
- 编码与空白：5 个变更文件 `BOM False / CR 0 / UTF8 True`；`git diff --check` 无输出、exit 0。
- 偏差：验收原文允许"单测或日志时序验证"，本轮采用仪器用例直接测量发射耗时与订阅生命周期，未额外制造主线程繁忙的 logcat 时序图。

## 阶段 1 · T1.3 deviceId 改为应用自管 UUID（W2 落地，P1·M）

变更文件：`HardwareProbeUtils.kt`（身份来源）、`DSimHardwareTester.kt`（删除私有副本）；新增 `app/src/androidTest/java/com/example/dsim/DeviceIdentityTest.kt`。

关键改动：

1. `getDeviceId` 不再读 `Settings.Secure.ANDROID_ID`：改为读独立 prefs `dSIM_IDENTITY` 的 `DEVICE_ID`；缺失时 `UUID.randomUUID().toString().replace("-", "")` 生成 32 位十六进制并 **`commit()`**（不是 `apply()`：身份必须先落盘，否则进程紧接着死亡会让下一个进程再铸一个 id，把同一台设备的历史劈成两半）。`commit()` 失败时留一条 WARN。
2. 进程级缓存 `@Volatile cachedDeviceId` + `synchronized(this)` 双检：33 个调用点（实测 `git grep getDeviceId(` 在 main 源集 36 处命中，含定义与 `DSimHardwareTester` 的私有副本）不再有跨进程 `Settings.Secure` 读。已核实清单里无 `android:process`，应用是单进程，因此进程内缓存即等价于安装级唯一。
3. 删除 `DSimHardwareTester` 的私有 `getDeviceId`（它输出 `"Android_ID: …"` / 备用 UUID 文案，与真实身份无关，会让自检报告误导排查），改为调用 `HardwareProbeUtils.getDeviceId`；随之删除该文件已不再使用的 `java.util.UUID` 与 `android.provider.Settings` 导入，以及 `HardwareProbeUtils` 的 `android.provider.Settings` 导入。
4. **决策（计划要求写明）**：不做旧 `ANDROID_ID` → 新 UUID 的数据迁移。项目无发布用户，旧 `sms_messages.deviceId` / `device_profiles` / `DEV_<id>` mappingKey 随卸载或覆盖安装后的首次运行自然重建；不写兼容分支（AGENTS §0 第 5 条：不预写兼容代码）。后果是**同一次安装内**身份稳定，**卸载重装**后身份改变（下方实测）。
5. 身份为**惰性创建**（首次 `getDeviceId` 调用时写盘），未新增 Application/Activity 首启钩子：计划只点名 `HardwareProbeUtils` 与调用点，且实测卸载重装后 `shared_prefs/dSIM_IDENTITY.xml` 在任何调用方运行前确实不存在，首次调用后即出现并此后不变（见证据）。若审查方要求"首启即生成"，需要另批一个初始化点。

验证（全部真实输出）：

- 构建与单测：`BUILD SUCCESSFUL in 1m 8s`（`:app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest`）、exit 0；`JVM tests=109 failures=0 errors=0`；`git diff --check` 干净；3 个变更文件 `BOM False / CR 0 / UTF8 True`。
- 新仪器用例 `DeviceIdentityTest`（emulator-5554）：
  - `deviceIdIsStableApplicationUuidNotAndroidId`：格式 `^[0-9a-f]{32}$`；等于 `dSIM_IDENTITY/DEVICE_ID`；反射清空进程缓存后再读仍相同；**不等于** `Settings.Secure.ANDROID_ID`；`buildNoRootMappingKey` 前缀为 `DEV_<deviceId>_SUBID_7` 且 `parseDeviceIdFromMappingKey` 往返一致（`SUBID` 与 `SLOT` 两种）。
  - `historyImportDeduplicatesOnRerunWithSameDeviceId`：真实系统库 40 条，`resetImportProgress` 后连续两次 `importQueuedHistory` → 行数 40→40，第二次 `imported=0`、`skipped=40`、`scanned` 两次相同。
  - 两个用例在两个**独立进程**中各跑一次（中间 `am force-stop`）：`OK (2 tests)` ×2，`dSIM_IDENTITY` 始终 `e789752a53d54893a1c649776accdb4c`。
- 验收①（debug 与 release 同机一致，emulator-5556，同一份数据）：先装 debug → `publishing on dsim/test/9f3a2b/7218351f6a5342608458192447933cc5`；再 `install -r` 覆盖安装 **release**（`assembleRelease -x lintVital*`，用 `zipalign` + `apksigner` 以 debug keystore 签名，证书 `CN=Android Debug`，SHA-256 `d89e4638…14333b`，因此数据得以保留）→ 同一日志行 `publishing on dsim/test/9f3a2b/7218351f6a5342608458192447933cc5`，**deviceId 完全一致**；release 不可 `run-as`（`package not debuggable`，符合预期）；随后装回 debug，身份仍是同一值。
- 验收②（卸载重装后重跑历史导入仍能命中去重，emulator-5554）：卸载前身份 `e789752a53d54893a1c649776accdb4c`、`dsim_core_database` 90112 字节；`adb uninstall` 后重装 → 身份文件不存在（尚未有调用方）→ 跑 `DeviceIdentityTest`：`OK (2 tests)`、`Time: 9.2`，日志 `historyImport deviceId=aa6b284f first(scanned=40,imported=40,skipped=0) second(scanned=40,imported=0,skipped=40) rows=40->40 system=40`，身份变为 **新** UUID `aa6b284f1c2249a38b18d6c32f1675e5`。即：新身份下 `findSimilarLocalMessage` 依然命中（第二次导入 0 新增、40 条跳过），符合审查 §2.8 的要求；同时如实记录"卸载重装 = 新设备身份"这一有意取舍。
- 验收③（回声过滤 / `isLocalDevice` / mappingKey 前缀）：mappingKey 前缀与往返已在仪器用例断言；`isLocalDevice` 与回声过滤属双机行为，按计划在阶段末 `scripts/verify-dsim.sh`（12/12，含互发现=双方 `device_profiles.isLocalDevice` 判定）与追加的自回声单行检查中统一实测，本节不提前下结论。
- 环境事故（非代码问题）：验证途中 emulator-5554 实例自行消失（宿主仅剩 5556 的 qemu 进程），按已知处置清 `dSIM_B.avd\*.lock` 后冷启动恢复；`session_act kill` 终止了卡在 `- waiting for device -` 的脚本并重跑，AVD 数据未丢（卸载前身份与数据库均如常读到）。

## 阶段 1 · T1.4 日志脱敏层（P1·M）

变更文件：新增 `app/src/main/java/com/example/dsim/DsimLog.kt`、`app/src/test/java/com/example/dsim/DsimLogTest.kt`；`app/src/main/java` 下 **24 个**文件的全部日志调用改为经 `DsimLog`（实测转换前 main 源集 `Log.d/w/e/i` 共 96 处、`android.util.Log.*` 全限定 12 处；转换后 `git grep -c -E '(^|[^.A-Za-z])Log\.[dewi]\(' -- app/src/main/java | wc -l` = **0**）。

关键改动：

1. `DsimLog` 提供 `d/w/e` 三级（计划要求）：`d` 在 `BuildConfig.DEBUG == false` 时为空操作（配合 T4.1 的 R8 规则整体剥除调用点）；`w/e` 始终输出但同样先脱敏；`d/w/e` 均有可选 `Throwable` 形参，保持既有 `Log.e(TAG, "msg", e)` 调用形态。
2. 统一脱敏 `redact()`：`(?<![0-9A-Za-z])\+?\d[\d\- ]{5,}\d(?![0-9A-Za-z])` 命中即按 `maskNumber` 处理（只留尾 4 位，星号最多 8 个以免泄露真实位数）。字母数字前后界是刻意的：deviceId / uuid / topic 后缀这类十六进制串里的数字段（例如 `7218351f…` 开头的 7 位数字）不会被误伤——设备日志实测仍是完整 topic。
3. `maskNumber()`（结构化号码显式脱敏）与 `fingerprint()`（SHA-256 前 8 位十六进制，用于关联同一条消息的多行日志）为公开工具方法。
4. 重点泄露点按计划逐处修：
   - `MqttInboundHandler`：`忽略无法识别的云端消息: ${decryptedJson.take(200)}` → `action=${actionHint(...)} length=${...} hash=${DsimLog.fingerprint(...)}`。`actionHint` 只从 JSON 里取 `"action"` 的值且字符类限死 `[A-Za-z0-9_]{1,40}`，取不到就是 `unknown`，**明文载荷不再进日志**。
   - `SmsReceiver`：`from=$cleanAddress` → `from=${DsimLog.maskNumber(cleanAddress)}`。
   - `SystemSmsStore` 两处：`address=$address` → 掩码。
   - `MainActivity` 历史同步两处、`SmsDatabaseTester` 一处：`${msg.address}` / `from=$sender` → 掩码。
5. `CloudSettingsManager` 唯一的 `android.util.Log.i` 归入 `DsimLog.d`（三级门面没有 info）。后果如实记录：release 下不再输出"migrated plaintext password to sealed storage"；与口令存储可用性相关的两条 `Log.w`（Keystore 不可用、密封值读不出）保留为 `w`，且 T1.5 会把明文降级变成用户可见警示。
6. 只改日志出口，不改任何业务分支、通知文案、Room 写入或协议；`InboundDispatcher` 的自回声日志、`MqttSyncService` 的订阅日志等仍保留（内容不含号码/正文）。

验证（全部真实输出）：

- 构建与单测：`:app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` → `BUILD SUCCESSFUL in 1m 5s`、exit 0；`JVM tests=113 failures=0 errors=0`（原 109 + 新 4）；`git diff --check` 干净；29 个变更/新增文件编码检查通过（无 BOM、无 CR）。
  - 首轮曾失败 2 例，均为**测试期望写错**而非产品缺陷：`+86 138-0013-8001` 的星号数按 8 位上限应为 8 个（原写 7 个）；十六进制 deviceId 中的 7 位数字段会被无边界正则误伤。修正方式：给正则加字母数字前后界（产品侧改进）+ 改正测试期望，随后 113/113。
- 新 JVM 用例 `DsimLogTest`（4 例）：11 位号码 → `*******8001`；带 `+86`/空格/连字符 → `********8001`（星号上限 8）；≤4 位全掩码；null/空白 → 空串；自由文本中号码被掩而 `action=SMS_RECEIVED`、`subId=1` 原样保留；deviceId/topic 行**完全不变**；`fingerprint` 稳定、8 位十六进制、null → `-`。
- 设备验收（真机链路，非仅单测）：
  - A. emulator-5554 注入真实入站短信 `adb emu sms send 13800138999 "T14_SECRET_BODY_7d9c2f64 code 246810"`：全量 logcat 中完整号码 `13800138999` 出现 **0** 次、正文标记 `T14_SECRET_BODY_7d9c2f64` 出现 **0** 次、掩码尾号 `8999` 出现 2 次；实际日志行 `D dSIM_Receiver: Captured incoming SMS action=android.provider.Telephony.SMS_DELIVER, from=*******8999, subId=2, slotIndex=0, mappingKey=DEV_aa6b284f…_SUBID_2_SLOT_0_UNBOUND`。应用数据库仍保存真实号码（只脱敏日志，不改数据）。
  - B. emulator-5556 收到一条**可解密但无法识别**的云端消息（宿主 Python 按 DSM3 加密 `{"action":"UNKNOWN_PROBE","marker":"SECRET_MARKER_7d9c2f64","address":"13800139999","body":"T14 cloud plaintext…"}`，经本机 Mosquitto 发布到 `dsim/test/9f3a2b/t14probe`，244 字节，QoS1 PUBACK 已确认）：logcat 中 `SECRET_MARKER_7d9c2f64` / `T14 cloud plaintext` / `13800139999` 各 **0** 次；处理端日志为 `W dSIM_SyncService: 忽略无法识别的云端消息: action=UNKNOWN_PROBE length=149 hash=f4b60645`。口令经 `DSIM_TEST_PASSWORD` 环境变量传入，未写入任何文件。
- 未做：release 包上的 logcat 实测（本轮 release 包在 T1.3 已构建，但 T1.4 之后未重新出包）；`d` 级在 release 为空操作由 `BuildConfig.DEBUG` 分支与既有变体机制保证，R8 剥除规则属 T4.1。

## 阶段 1 · T1.5 Keystore 明文降级的用户可见化（P1·S）

变更文件：`CloudSettingsManager.kt`、`CredentialVault.kt`、`SettingsActivity.kt`、`res/layout/activity_settings.xml`；新增 `app/src/androidTest/java/com/example/dsim/CredentialStorageWarningTest.kt`。

关键改动：

1. `CloudSettingsManager.isPasswordPlaintextFallback(context)`：当 `dSIM_UI_PREFS.PASSWORD`（旧明文键）非空时为 true，即"`seal` 被拒后仍在明文存储"的唯一状态来源。`saveConfig` 的降级分支**保持原样**（仍允许保存，不把用户锁在外面），`readPassword` 每次读取都会重试密封，因此 Keystore 恢复后状态自动消失——这两点是既有 C23 语义，未改。
2. 设置页云配置区新增持久警示 `tvCredentialStorageWarning`（`activity_settings.xml`，紧跟 `tvCloudConnectionStatus`，默认 `visibility="gone"`，红字红底）；文案为计划指定的"口令当前以明文存储（本机 Keystore 不可用）"。
3. `SettingsActivity.refreshCloudConnectionUi()` **开头**统一刷新该警示：函数在本地模式分支会提前 `return`，放在开头才能保证任何使用模式下都显示（消除静默降级）。
4. `CredentialVault` 增加 `internal var sealOverride: ((String) -> String?)?`：仅测试替身入口，`seal()` 首行委托；生产代码任何位置都不赋值（`git grep sealOverride` 仅命中 `CredentialVault.kt` 与测试）。这是为了让"Keystore 拒绝密封"能在 Keystore 正常的设备上被真实执行，而不是用 mock 框架（项目未引入）。

验证（全部真实输出）：

- 构建与单测：`:app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest` → `BUILD SUCCESSFUL in 2m 9s`、exit 0；`JVM tests=113 failures=0 errors=0`；`git diff --check` 干净；变更文件编码检查 `encoding problems: []`。
- 新仪器用例 `CredentialStorageWarningTest`（emulator-5554）→ `OK (1 test)`、`Time: 17.689`。用例在真实 `SettingsActivity` 上断言（`ActivityScenario` + `onActivity` 读取真实 TextView），并完整快照/还原 `dSIM_UI_PREFS`：
  - 测试替身让 `seal` 返回 null → `saveConfig` 仍成功，`getConfig().password` 仍等于原口令（可用性未破坏），prefs 中 `PASSWORD` 存在、`PASSWORD_ENC` 为 null，`isPasswordPlaintextFallback=true`、`isPasswordSealed=false`，设置页警示 **VISIBLE** 且文案逐字等于"口令当前以明文存储（本机 Keystore 不可用）"。
  - 撤掉替身（正常设备路径）→ 再次 `saveConfig` 后 `isPasswordSealed=true`、`isPasswordPlaintextFallback=false`、口令仍可解出，设置页警示 **GONE**。
  - `finally` 中停止可能拉起的服务并还原 prefs；测试后实读 `dSIM_UI_PREFS.xml` 为 `<map />`（与测试前一致，未留残留配置）。
- 未做：真实 Keystore 故障设备（本机模拟器 Keystore 正常，故用替身注入失败，符合计划验收写法）；release 包上的界面复核（本轮 release 包出在 T1.3，T1.4/T1.5 之后未重新出包）。

## 阶段 1 · 阶段末全量回归（T1.1–T1.5 之后）

命令与环境：Git Bash（`C:\Program Files\Git\bin\bash.exe`）+ `JAVA_HOME=C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot` + `-Dorg.gradle.java.installations.paths=...`；设备 emulator-5554（dSIM_B，ICC sim_a.xml）与 emulator-5556（dSIM_C，ICC sim_b.xml），本机 Mosquitto（`dsim-mosq`，`tcp://10.0.2.2:1883`，topic `dsim/test/9f3a2b`）。完整日志在仓库外证据目录：`stage1-regression.log`、`stage1-dualdevice.log`、`stage1-dualdevice2.log`。

1. **全套仪器测试**（`ANDROID_SERIAL=emulator-5554 ./gradlew :app:connectedDebugAndroidTest`）：
   `dSIM_B(AVD) - 16 Tests 20/20 completed. (0 skipped) (0 failed)` → `Finished 20 tests` → `BUILD SUCCESSFUL in 3m 49s`。
   20 = 阶段 0 的 13（Example 1 / ReplayDelivery 2 / SendCommandLedger 5 / SyncOutboxDao 5）+ 本阶段新增 7（MqttLifecycleResponsiveness 1 / RadarFlowLifecycle 3 / DeviceIdentity 2 / CredentialStorageWarning 1）。
2. **计划原文全局回归命令**（`:app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest`）：`BUILD SUCCESSFUL in 18s`，XML 汇总 `FINAL JVM tests=113 failures=0 errors=0`（阶段 0 的 109 + T1.4 新增 4）。
3. **双机回归 `scripts/verify-dsim.sh emulator-5554 emulator-5556`**：`PASS=12 FAIL=0`、`VERIFY_EXIT=0`。逐项：SIM 挂载 LOADED/slot0；采集 `action=android.provider.Telephony.SMS_DELIVER`；落库 `id=1 type=1 status=1 simId=2`、`mappingKey=ICCID_89860318640220133814`、正文一致、`uuid` 唯一索引存在；通知 `channel=dsim_loud_v1`；前台服务在跑；跨设备互发现 `5554 local=63fcfafde93645a0aa48cdf5e227501f` / `5556 local=fa9e4551fede4c97935733e363a02b53`、两个 deviceId 不同、`mutual discovery OK`。
4. **T1.3 验收③补充取证（自回声 / isLocalDevice / 跨设备单份落库）**：在 5554 注入 `STAGE1_XDEV_1789833464 code 246800`：
   - 5554 `skip own echo` 日志 1 行、5556 0 行；
   - 两台各恰好 1 行同 UUID `fb2add1a-bc37-40d8-ad42-ab9c2da3758f`、`deviceId=63fcfefd…`（发送方身份）、`status=1`，`sync_outbox` 均为 0（已交付并清空）；
   - `device_profiles`：5554 = [(对端 id,0,REMOTE),(本机 id,1,LOCAL)]、5556 = [(对端 id,0,REMOTE),(本机 id,1,LOCAL)]，**各自 local_count=1**，即 `isLocalDevice` 判定正确。
   - 日志中 `mappingKey=ICCID_********3814`（T1.4 掩码生效）而数据库内仍为完整 `ICCID_89860318640220133814`，即脱敏只作用于日志。
5. **回归过程中两次 harness 失败（非产品缺陷，均如实记录）**：
   - 第一次 `verify-dsim.sh` 得到 `PASS=8 FAIL=1`（`no mutual discovery`）。根因：脚本只对 `$SERIAL`（5554）执行 `am start`，而 5556 的应用在 T1.5 步骤里被 `install -r` 杀掉后没有被重新拉起，因此 5556 根本没连上 broker；5554 侧证据正常（自回声 1 行、单份落库、outbox 清空）。处置：显式拉起两端并等待双方 `subscribed dsim/test/9f3a2b` 后重跑 → 12/12。
   - 第二次脚本在 `connectedDebugAndroidTest` 之后立即跑双机：该 Gradle 任务结束时会**卸载**两个 APK（实测 `pm list packages | grep -c` = 0），导致 `com.example.dsim not installed`。处置：重新 `onboard-device.sh` 后再跑 → 12/12。
6. **升级安装的既有数据后果（T1.3 相关，需裁决，未擅自修）**：5556 在 T1.3 之前已按 ANDROID_ID 建过档案，覆盖安装新构建后 `device_profiles` 实测为
   `[('d9e0118b7d57f7c6',0,'REMOTE'), ('96fad9a2276863b0',1,'LOCAL'), ('7218351f6a5342608458192447933cc5',1,'LOCAL')]`
   ——**同一台设备出现两条 `isLocalDevice=1`**（旧 ANDROID_ID 身份 + 新 UUID 身份），设备中心会多出一个"本机"幽灵条目，`verify-dsim.sh` 的 `local_a[0]` 取值也变得不确定。计划明确"不做 ANDROID_ID→UUID 迁移、旧数据可弃"，因此本轮**只记录不改**：为取得干净的 12/12，对 5556 执行了 `pm clear` + 重新 onboard（测试设备、无用户数据）。若需要面向"覆盖安装"的清理（例如首启删除 `deviceId != 当前身份` 且 `source=LOCAL` 的档案行），请批准后另开任务；它不属于阶段 1 任何一项的既定范围。
   - **对端影响面补记（2026-09-20，审查 F-4）**：残留不只在覆盖安装的本机——对端数据库里同样留有旧身份的 `DEV_<旧 ANDROID_ID>_…` REMOTE_SHADOW 卡与一条永远离线的幽灵设备行；即使本机 `pm clear` 重装，对端的这批影子也不会自行消失（它只知道旧设备再没上线）。一次性清理需双机各执行一次，已登记为 `REFACTORING.md` W23（P3 可选、未批准）。
7. **工作区状态**：`git status --porcelain=v1` 仅剩审查方 3 个修改（`.active_plan` 与两个 2026-09-18 `task_plan.md`）、只读未跟踪文档（`代码质量审查/`、`修复计划_2026-09-19.md`、`claude-fable-5.1-high · 001.md`、`.planning/2026-09-19-code-review-track/`）、两个技能冒烟目录、以及本阶段账本目录 `.planning/2026-09-19-repair-plan-stage1/`。`tmp_dbpull/`（含设备短信库）已删除。全程未执行 `git stash` / `checkout` / `clean`。
8. **提交与推送状态**：本阶段 5 个任务提交 `d981457 / 9a87a46 / 46e2bd3 / 3b0e1c2 / cefd02a`，`git rev-list --left-right --count origin/main...main` = `0	5`，**未 push**（阶段 0 先例：push 属对外动作，需用户批准）。

---

# 2026-09-20 阶段 1 审查后跟进：F-1 ~ F-5

范围来源：`代码质量审查/阶段1执行审查_2026-09-20.md`。D6 由 F-2 收口；D1–D5 等用户确认，本轮未动。共 5 个 conventional commit，前方另有一次对 F-1 提交的 amend（见 F-1 记录）。

## F-1（commit `e4f5ea1`，docs(agents)）文件树治理

1. 新增 `scripts/check-agents-tree.sh`（50 行）：遍历 `app/src/main` 全部 `.kt`（含 `database/`），凡文件名未出现在 AGENTS.md §5 文件树代码块（awk 按节提取，找不到节或块即失败）即列名并 `exit 1`；例外需在脚本顶部显式列出（当前为空：要求 100% 进树）。
2. 首跑真实输出：`AGENTS.md §5 文件树缺少以下 10 个 main Kotlin 源文件条目： CloudConfigMessages.kt / CloudTopics.kt / CredentialCodec.kt / CredentialVault.kt / DsimLog.kt / HeartbeatPolicy.kt / NotificationPreferences.kt / ReconnectPolicy.kt / ReplayGuard.kt / SendCostPolicy.kt`，`EXIT=1`。已把 10 个条目（含本轮新增的 `DsimLog.kt`）按语义分组补进文件树并对齐缩进。
3. 易失标注处理（按"删掉易失数字、指向代码本身"）：文件头"55 个 Kotlin 文件 / 约 14,300 行"改为"刻意不写死 …以源码与 §5 文件树为准"；`MqttSyncService` 的"（W4 后 568 行）"标注删除（实测 736）；核对时发现 `DsimDao.kt "40 个 DAO 方法"`同为漂移标注（实测 46 个 `fun`），按同一理由改成"DAO 收口"。`6 个 @Entity`为审查方刚验证过的正确值，未动。
4. 验收：`bash scripts/check-agents-tree.sh` → `OK：AGENTS.md §5 文件树覆盖全部 74 个 main Kotlin 源文件。`、`EXIT=0`；临时新建 `app/src/main/java/com/example/dsim/ZzTreeCheckProbe.kt`（用编辑工具创建）后复跑 → 报缺失、`EXIT=1`，删除探针后 → `EXIT=0`（未提交，工作区无残留）。两文件逐字节检查 `BOM False / CR 0`。首版提交后目检发现新增 10 行缺 8 空格树缩进，已修复并对该提交执行 `--amend --no-edit`（未推送过，公共历史无影响），最终提交为 `e4f5ea1`。

## F-2（commit `4ec7c3f`，fix(credentials)）sealOverride 测试接缝加保护

- `CredentialVault.sealOverride` 加 `@VisibleForTesting`（`androidx.annotation`，项目已有该依赖先例）；setter 增加 `check(BuildConfig.DEBUG) { "sealOverride is a debug/test-only seam (F-2)" }`——release 构建永远无法赋值（赋值即 throw），误用不会静默绕过 Keystore。D6 就此收口。
- 验收：`:app:compileDebugKotlin :app:assembleDebug` → `BUILD_EXIT=0`。

## F-3（commit `e5b7415`，docs(agents)）C7 唯一例外登记

- AGENTS.md C14 行后新增："> **C7 已登记的唯一例外（F-3）**：切到本地模式时，`APPLY_LOCAL_MODE` 的退役路径仍会经 `publishOfflineBestEffort` 向群 topic 发一条可解密的 OFFLINE，**不过 C7 闸**。理由：离开通知必须在断开连接前发出，否则对端会把本机长期显示为在线（LWT 只是服务端兜底，靠它意味着最长数分钟的假在线）。"
- 未改动 C7 表行本身，其余新增上传点仍以过闸为准。

## F-4（commit `b84f56b`，docs(fixes)）对端影响面补记 + REFACTORING W23

- FIXES 阶段 1 回归第 6 条（F-T1.3-upgrade）补记：**对端**数据库同样残留旧身份 `DEV_<旧 ANDROID_ID>_…` REMOTE_SHADOW 卡与离线 ghost 设备行，本机清理不会让对端影子消失。
- REFACTORING.md：总览表新增 `W23 P3（可选·未批准）`；详情节登记现象/裁决背景/最小边界（一次性 grep 可见的清理、双机各执行一次、**不得**为此新增在线协议消息）。始终可选，未获批准前不写代码。

## F-5（commit `fc70d45`，docs(logging)）redact() 取舍写入 KDoc

- `DsimLog` KDoc 新增第 3 条：redact() 把**一切** ≥7 位数字串打码（含 ICCID/subscriptionId，属硬件标识是好事）；排查 SIM/subscription 问题时原值不会进 logcat，改用 `fingerprint()` 按哈希关联。

## 本轮回归与验收

- `JAVA_HOME=…/jdk-21.0.7.6-hotspot` + `-Dorg.gradle.java.installations.paths=...`：`:app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest` → `BUILD SUCCESSFUL in 38s`、`FINAL JVM tests=113 failures=0 errors=0`。
- `:app:lintDebug` 按既有 5 error 如期 `BUILD FAILED`（不为通过改产品代码）；文本报尾部 `5 errors, 312 warnings`（与审查基线 5/312 完全一致）、`NewApi_count=0`。
- 全程 `git diff --check` 干净；所有新改文件 UTF-8 无 BOM、LF。
- 偏差如实记录：起手发现 `.planning/.active_plan` 被前一轮技能烟测误写为 `…arena-skill-check…`（正是任务书所指那笔"已修改"），已按任务书意图改回 `2026-09-19-repair-plan-stage1`；该文件仍属未提交脏项，按医嘱不在本轮提交。

---

# 2026-09-20 阶段 1 收尾审查跟进：F-6 / 防复发 / F-7 / F-8

范围来源：`代码质量审查/阶段1收尾审查_2026-09-20.md` + 本轮用户指令。F-1~F-5 不重做；D1–D5、W23 不动；W22 串行设计不改；不 push、不开始阶段 2。本段 F-6 是本次收尾审查编号，不是本文早期阶段 0 的 F6。

## 起点与隔离

- `git log --oneline -8`：HEAD=`8ee0cf5`；`git status`：ahead 15；`git rev-list --left-right --count origin/main...main`：`0 15`。
- `adb devices`：emulator-5554 / emulator-5556 均为 device。`adb -s emulator-5554 shell pm list packages com.example.dsim` **空输出**；5554 确实无应用。没有预装、没有手动 pm grant、没有先走 onboarding。
- 每次命令显式固定 `PLAN_ID=2026-09-19-repair-plan-stage1` 与项目 `PWF_PLAN_ROOT`，使用既有三件账本；不新建计划目录、不调用 init-session、不改 .active_plan 或审查方账本。
- 对既有其他计划、审查文档、只读资料及 app/src/main 共 217 文件记录路径+内容联合 SHA256，收尾复算完全相同：`de7714eb2fd0a8afe50374e6085140354bc015f3f9e2b2d72c6c355353430cac`。

## F-6（commit c66a18b）新装设备上的测试权限和数据前提

文件：`app/src/androidTest/java/com/example/dsim/DeviceIdentityTest.kt`、`app/build.gradle.kts`。

关键 diff：
- 测试类增加 `@get:Rule val smsPermissionRule: GrantPermissionRule = GrantPermissionRule.grant(Manifest.permission.READ_SMS)`；只需要 READ_SMS，本次没有增加其他权限规则。
- `systemCount > 0` 从 `assertTrue` 改为 `Assume.assumeTrue`（显式导入 assumeTrue）：空系统库属于缺失 fixture，应跳过，不作为导入失败；没有播种/删除系统短信。
- Gradle 依赖报告确认原测试编译类路径没有 rules；新增仅 `androidTestImplementation("androidx.test:rules:1.5.0")`，与现有 AndroidX Test 1.5 系列对齐。未改 main 依赖或产品代码。

验收命令（由仓库外 `f6-fresh-install-20260920.sh` 执行；进入 Gradle 前再次断言包查询为空）：

```bash
export PLAN_ID=2026-09-19-repair-plan-stage1
export PWF_PLAN_ROOT=C:/Users/admin/AndroidStudioProjects/dSIM
export JAVA_HOME="/c/Program Files/Microsoft/jdk-21.0.7.6-hotspot"
export ANDROID_SERIAL=emulator-5554
./gradlew :app:connectedDebugAndroidTest \
  -Dorg.gradle.java.installations.paths="C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot" --console=plain
```

真实输出（session-bbbd4ed9696345c78e55717c，exit 0）：

```text
F6_FRESH_INSTALL_PRECONDITION=PASS (no com.example.dsim packages; no preinstall or pm grant)
Starting 20 tests on dSIM_B(AVD) - 16
dSIM_B(AVD) - 16 Tests 20/20 completed. (0 skipped) (0 failed)
Finished 20 tests on dSIM_B(AVD) - 16
BUILD SUCCESSFUL in 3m
F6_CONNECTED_EXIT=0
```

XML 独立核对：`tests=20 failures=0 errors=0 skipped=0`；`historyImportDeduplicatesOnRerunWithSameDeviceId` 有 testcase 且没有 skipped/failure/error 子节点。用例日志：`first(scanned=46,imported=46,skipped=0) second(scanned=46,imported=0,skipped=46) rows=46->46 system=46`。因此不是靠 Assume 跳过刷绿：本机有46条短信，导入与去重断言确实执行。

结论：F-6 新装设备全套验收通过。空库 Assume 分支已实现，但本机未触发，不声称另外做过空库设备实测。UTP 结束后5554包查询仍为空，未重新安装应用。

历史纠正：前次 T1.3 的 `pm grant` 是当时的绕行处置，不能证明新装测试自足；本次 F-6 确认并修复的是测试前提声明缺失，未把产品代码作为修复对象。

## 防复发（commit 23c7e99）回归约定

文件：`AGENTS.md` §8.3。只追加一条约定：**每轮回归必须包含 `:app:connectedDebugAndroidTest`，且该轮至少一次在全新安装或清数据设备上执行**；多设备用 ANDROID_SERIAL 选单台。理由明确为运行期权限、系统短信等设备状态依赖，不能用已授权设备历史绿灯或 JVM 单测替代。

验证：`git diff -- AGENTS.md` 仅新增该段；`git diff --check` exit0。执行证据就是上面的本轮新装20/20，不引用旧轮次绿灯。

## F-7（commit 21fea7d）树脚本完整文件名边界

文件：`scripts/check-agents-tree.sh`。把 `grep -qF "$name"` 改为先转义 ERE 元字符、再按 `(^|[[:space:]])${name_regex}([[:space:]]|$)` 匹配独立文件名 token。只改匹配，保留 §5 围栏抽取与既有失败检查。

用编辑工具创建 `app/src/main/java/com/example/dsim/Topics.kt`（与既有 CloudTopics.kt 构成子串），运行同一命令 `bash scripts/check-agents-tree.sh`，再用编辑工具删除。真实输出：

```text
# 修前：复现旧脚本假通过
OK：AGENTS.md §5 文件树覆盖全部 75 个 main Kotlin 源文件。
BEFORE_FIX_WITH_TOPICS_PROBE_EXIT=0
# 修后、探针尚在
AGENTS.md §5 文件树缺少以下 1 个 main Kotlin 源文件条目：
  app/src/main/java/com/example/dsim/Topics.kt
AFTER_FIX_WITH_TOPICS_PROBE_EXIT=1
# 删除探针后
OK：AGENTS.md §5 文件树覆盖全部 74 个 main Kotlin 源文件。
AFTER_PROBE_REMOVAL_EXIT=0
TOPICS_PROBE_EXISTS=False
```

`bash -n scripts/check-agents-tree.sh` 与 `git diff --check` 均 exit0。探针未提交、无残留；F-7 验收通过。

## F-8（commit 8fe7eb9）历史基线不改写

文件：`REFACTORING.md`，仅 §1 新增一句：活口径以 `AGENTS.md` §5 文件树 + `scripts/check-agents-tree.sh` 为准，历史数字仅存档即可。

验证：用 Python 将当前文本仅移除新增句及空行，再与 `git show 8ee0cf5:REFACTORING.md` 逐字比较，真实输出 `F8_HISTORICAL_CONTENT_UNCHANGED=True`。commit 0f6b445 的55文件/14,245行/503函数，以及历史行59文件/~14,700行均原样保留。`git diff -- REFACTORING.md` 仅两行新增（一句+空行）；F-8 验收通过。

## 本轮收尾回归与限制

同上 JDK21 和 installations.paths 环境，执行：
1. `./gradlew :app:compileDebugKotlin :app:cleanTestDebugUnitTest :app:testDebugUnitTest --console=plain` → `BUILD SUCCESSFUL in 22s`、`F6_F8_COMPILE_JVM_EXIT=0`；17份XML汇总 `tests=113 failures=0 errors=0 skipped=0`。
2. `./gradlew :app:lintDebug --console=plain` → **`BUILD FAILED in 1m 3s`、`F6_F8_LINT_EXIT=1`**；`5 errors, 312 warnings`，XML确认 MissingSuperCall×1 + PermissionImpliesUnsupportedChromeOsHardware×4，NewApi=0。这是既定存量失败，未为通过修改产品代码、未改lint基线。
3. 仓库外 `f6-f8-final-verify-20260920.py` → `ENCODING files=9 UTF8=True BOM=0 CR=0 final_LF=True`；`PRODUCT_MAIN_DIFF_EMPTY=True`；`DIFF_CHECK_WORKTREE_AND_COMMITTED_RANGE=PASS`；217份受保护文件摘要一致。
4. 没有改动采集/同步业务逻辑，本轮没有额外做双机E2E、低API或真机验收，不用本轮结果替代这些未执行项。没有新问题需扩范围裁决；push、D1–D5、W23、阶段2仍维持待批准。

辅助执行观察：可选 rules 缓存路径检查因路径不存在 exit1，随后实际 Gradle 构建成功；F-8 首次 Python 默认控制台编码使中文显示乱码，已用 PYTHONIOENCODING=utf-8 复核，源文件 UTF-8 字节与历史全文比较均正常。以上不隐瞒，但均未改变验收结论。

证据目录（仓库外，不入git）：`C:\Users\admin\AgentDock\dsim-stage1-evidence\`。
- `f6-fresh-install-20260920.sh`、`f6-fresh-install-20260920-part1.log`、`f6-fresh-install-20260920-part2.log`：运行前提与完整分段控制台输出。
- `f6-instrumented-result-20260920.xml`、`f6-report-verification-20260920.log`：原始XML与目标用例核对。
- `f7-boundary-probe-20260920.log`、`f8-history-preservation-20260920.log`：双向探针与历史内容核对。
- `f6-f8-regression-20260920.sh/.log`、`f6-f8-final-verify-20260920.py/.log`：编译/JVM/lint与编码/保护摘要核验。

四项任务各自独立 conventional commit；FIXES 与既有三件账本在单独收尾提交中记录。全程不 stash/checkout/clean、不 push；结束时停下等审查，不开始阶段2。
