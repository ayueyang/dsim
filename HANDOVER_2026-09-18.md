# dSIM 交接文档（2026-09-18）

> 由 `claude-fable-5.1-high · 001.md`（AI 会话记录，28 条消息）整理而成。
> 2026-09-18 续：批次 C、D、**E（工程）** 均已完成并提交（见 §3、§3a）。
> 项目：`C:\Users\admin\AndroidStudioProjects\dSIM`（Android / Kotlin / Room / Paho MQTT）

---

## 1. 一句话现状

批次 A（可靠同步）、B（省电/加密/心跳）、C（一行级加固）、D（安全）、**E（工程：死代码 / 协议类型化 / 服务拆分 / 错误处理 / 静态量收口 / 控制消息进发件箱）** 均已实现、验证、提交；HEAD = `d3c49ee`，工作树干净（仅剩本文件与会话记录两个未跟踪文件），**全部未 push（ahead 22，基线 `fdaf92d`）**。今天合计 17 个提交，其中批次 E 6 个（`0d8f675`→`d3c49ee`）。

**当前状态：等用户审查今天全部变更，暂不开新工。** 审查指引见 §8；审查后的下一步候选见 §5。

## 2. 用户已明确的产品前提（影响优先级，勿再问）

| 前提 | 含义 |
|---|---|
| 运营商发短信收费（约 ¥0.1/条），入站短信走互联网同步免费 | 产品核心 = **收到的短信不丢地同步到其他设备**；任何自动重试/重复发送都是花冤枉钱 |
| **不做**运营商短信转发（用短信转短信） | 70 字限制不够用，默认有网络环境 |
| 「本机 SIM 直发」不花额外费用，但非核心 | 降级到 P2 |
| 提交策略 | **提交前审阅、不推送**（本地 commit 即可，不要 `git push`） |

## 3. 已完成工作（按提交）

| 提交 | 内容 | 验证 |
|---|---|---|
| `d9f4dde` + `88c3883` | 第一批：`SEND_CMD` at-most-once 认领（`send_commands` 表，Room v6）、真实发送回调、Broker 配置恢复 | JVM 14/14 |
| `81a786b` + `c765de2` | **批次 A**：Room v6→v7 新增 `sync_outbox`；`SyncOutbox.kt` 同事务入队 + 单飞冲刷（PUBACK 后才删行）；`SmsReceiver` 只入队并发 `ACTION_FLUSH_OUTBOX`；`MqttSyncService` 固定 clientId `dSIM_<deviceId>`、`cleanSession=false`、文件持久化；通知栏「N 条待同步」 | JVM 21/21；仪器 10/10（含 v6→v7 迁移）；E2E 断网→注入 2 条→恢复→`sent=2 remaining=0` |
| `a78cf09` + `0c86a5d` | **批次 B**：加密格式 **DSM3**（`[DSM3][12B IV][GCM]`，AAD=魔数；主密钥 PBKDF2 固定盐 `dSIM/v3/master-key` 12 万轮，每口令派生一次，LRU 4 缓存；DSM2 读取路径删除）；发布 `<base>/<deviceId>`、订阅 `<base>/+`，解密前按 topic 丢自身回声；`HeartbeatPolicy` 30 s tick、指纹变化或 ≥120 s 才发 PONG，`ONLINE_TIMEOUT_MS` 45 s→5 min；OFFLINE 动作 + LWT；去掉 PONG Toast | JVM 39/39；E2E：回声 0 解密、静置 4 min 仅 keepalive、LWT 被外部 paho 收到并用 Python 交叉解密成功 |

| `f64188a` | **批次 C**：F5 `SmsReceivedReceiver` 补 `BROADCAST_SMS`；F14 新增 `.gitattributes`（`* text=auto eol=lf`，`*.bat`/`gradlew.bat` 保 CRLF）并重新检出 17 个文件；W15 `<activity .MainActivity>` 移入新建的 `app/src/debug/AndroidManifest.xml`、`SettingsActivity` 入口用 `BuildConfig.DEBUG` 包裹（顺带打开 `buildFeatures { buildConfig = true }`，AGP 8 默认不生成 BuildConfig） | JVM 39/39；`assembleDebug`+`assembleRelease` PASS；aapt2：release 的 MainActivity 引用数 0 / debug 为 2，两变体两个接收器均带权限；模拟器：让出 SMS 角色后真实短信经 `SMS_RECEIVED` 到达并 `outbox sent=1 remaining=0`，恢复角色后 `SMS_DELIVER` 回归正常，debug 包「设置→测试功能」仍可进入 |

| `842a32a` | **批次 D（安全）**：F8 备份与换机迁移改 deny-by-default（两份规则文件排除全部 domain，`<device-transfer>` 单列，因其不受 `allowBackup` 约束）；F12/F16 13 处 `dSIM_UI_PREFS` 直读收口（凭据 → `CloudSettingsManager.getConfig`，`IS_MUTED` → 新增 `NotificationPreferences`）；F9 默认 Broker 改 `ssl://broker.emqx.io:8883`（**存量配置不迁移**，仅新装生效）+ 新增 `validateBaseTopic` 在三个保存入口拒绝 `+`/`#`/空白/控制符/前导 `/`/`//`/超 200 字 | JVM 52/52（新增 `CloudSettingsManagerTest` 13 例）；`assembleDebug` PASS；模拟器：`/proc/net/tcp6` 出现 uid 10227 → `:22B3`(8883) ESTABLISHED + `subscribed <base>/+`，随后真实入站短信 `outbox sent=1 remaining=0`；备份做 A/B 对照（deny → `PFTBT Error -1002 rejected`；临时宽松规则控制组 → `status: 0`），对照后已还原 |

文档已同步：`AGENTS.md`（C3 改 DSM3，新增 C11/C12/C13/C14/C15）、`FIXES_2026-09-18.md`（第 1–3 批）、`REFACTORING.md`（W1 与 FIXES 矛盾已修正、度量已刷新、新增 W19–W21、批次表重排）、`TESTING.md` §5.4、架构说明书 §8.1/8.2/R4。

## 3a. 批次 E（工程）— 6 个提交，全部本地

基线 `842a32a` → `d3c49ee`：`app/src` 29 文件，+1194/−1171（净 +23 行，但 `MqttSyncService` 从 1102 行降到 ~560 行）。JVM 单测 52 → 64。每个提交都经过：JVM 全绿 → `assembleDebug` + `assembleRelease`（跳过 lintVital）→ 模拟器 emulator-5554 真实入站短信 / MQTT 互操作探针 → c8check + `git diff --check` → 本地 commit → AgentDock checkpoint。

| 提交 | 工作项 | 内容 | 验证 |
|---|---|---|---|
| `0d8f675` | W10/W11/F13 | 删 `DsimMqttEngine`/`DsimNetworkEngine`（hivemq 客户端，零调用者）及 `com.hivemq:hivemq-mqtt-client:1.3.3` 依赖；删未用 DAO 方法、`SendCmdPayload.kt`、`item_sms.xml` | 62/62；两变体构建；模拟器往返 |
| `5fbcdca` | W5 | 新增 `MqttProtocol.kt`：`MqttAction` 常量 + `SendCmd`/`SendCmdResult`/`HistorySyncAckMsg`/`Ping`/`Pong`/`Offline`/`HistoryQueueBatch`/`SmsSync` 等 data class（都实现 `MqttInbound`），`MqttPayloadCodec.encode/decode`（decode 对畸形 JSON / 未知 action 返回 null 不抛）。删除 `ENCRYPTION_ERROR` 哨兵字符串，`DsimCryptoUtils.encryptOrNull` 返回可空。新增 `MqttPayloadCodecTest`（含旧格式 JSON 兼容） | 62/62；探针发旧格式 PONG（全键、position null）被正确解析 |
| `8ba382e` | W4 步骤 1 | 从 `MqttSyncService` 抽出 `CloudSession`（broker/topic/password，`@Volatile`）、`MqttPublisher`（出站控制消息 + 心跳指纹状态，C13/C14 落在这里）、`MqttInboundHandler`（`handleIncomingMessage` 分发 ①–⑯ 顺序原样保留）。服务只剩生命周期、连接、通知、outbox 冲刷 | 62/62；PING→PONG、入站短信同步 |
| `62e133c` | W6 | 18 处 `catch (_: Exception) {}` / `printStackTrace` 归零：OEM 探测回退 `Log.d` + 注释，真实失败 `Log.w(e)`；新 tag `dSIM_Probe`/`dSIM_OtpRules`/`dSIM_SmsList`/`dSIM_HistoryImport`/`dSIM_DeviceManager`。`SyncOutbox.FlushResult.lastError`；冲刷失败时通知栏「N 条待同步，上次发送失败（原因），将自动重试」 | 62/62；断网→Paho `Connection lost (32109)`→自动重连→断网期间短信 `sent=1` |
| `7c2d55d` | W7 | `globalMqttClient` 改 `private @Volatile`，服务外 0 处触碰；删 `staticTopic`，只留 `staticConfig`（订阅成功写、三条 teardown 清空）。新增唯一外部发布入口 `MqttSyncService.publishToGroup(context, json, topic, password)`（校验 canUseCloud + 已连接 + topic **和** password 都等于当前组，再加密、`publishTopic` 发布；失败 false + 日志）。迁移 7 个直发点（雷达 PING、聊天页 SEND_CMD、历史队列批次、`SystemSmsHistoryImporter.CloudPublisher`、MainActivity 调试页 ×2）。删无调用者 `publishEncryptedSms` 和 **`"ACTION_PUBLISH_MSG"` intent 分支（它把调用方 payload 直发任意 topic、不校验组——安全漏洞）**。`MqttPublisher` 通过 `client: () -> MqttClient?` 取客户端 | 62/62；PING→PONG 2.3 s，SMS 同步 1.6 s |
| `d3c49ee` | W21 | `SyncOutbox.enqueueControl(kind, uuid, discriminator, json, group)` + `requestFlush()`；行键 `"$kind:$uuid:$discriminator"`。`OutgoingSmsDispatcher.publishOutcome`（SEND_CMD_RESULT + 已发短信 silentSync 同步）、`MqttPublisher.publishHistorySyncAck / publishSendCommandResult`（变 suspend，只组装入队）全部走发件箱。删 `publishCurrentGroup`。通知文案「N 条短信待同步」→「N 条消息待同步」 | 64/64（+2）；历史行→ACK 2.1 s、重放→`already_exists` 2.3 s、SEND_CMD→RESULT(SENT)+同步 1.9 s（`sent=2`）、断网入站→重连 `sent=1` |

文档同步：`REFACTORING.md`（W4/W5/W6/W7/W10/W11/W21 条目改 ✅ 并写明已做/未做/验证，度量表刷新）、`AGENTS.md`（文件树含三个新文件；C11 措辞改为「用 `publishToGroup` / `enqueueControl`」）、`.planning/2026-09-18-engineering-cleanup/{task_plan,progress}.md`。

**W4 的步骤 2（连接状态机 / `ConnectionManager`）与 W6 的「Result 类型」步骤 2 明确延后**，REFACTORING.md 已注明。

## 4. 新引入的不变量（改代码前必读）

- **C15**（批次 C）：两个短信接收器都必须带 `android:permission="android.permission.BROADCAST_SMS"`。验证只能用真实入站短信；`adb am broadcast` 是 protected broadcast，改动前后都被拒，不能作为证据。
- 调试组件只放 `app/src/debug/AndroidManifest.xml`，release 清单不得声明 `MainActivity`；`buildFeatures { buildConfig = true }` 不能关（`SettingsActivity` 用 `BuildConfig.DEBUG`）。
- 行尾以 `.gitattributes` 为准：除 `*.bat`/`*.cmd`/`gradlew.bat` 外一律 LF。改了属性后必须重新检出，`--renormalize` 单独用不会重写工作区。

- **C16**（批次 D）：备份规则必须 deny-by-default，排除**所有** domain，不要改成逐文件点名——备份规则是 allow-by-default 语义，点名清单会在下次改存储名时静默失效。`<cloud-backup>` 与 `<device-transfer>` 都要写。
- **C17**（批次 D）：房间号在保存入口必须过 `CloudSettingsManager.validateBaseTopic`；云端凭据（BROKER/TOPIC/PASSWORD）一律经 `CloudSettingsManager` 读写，不要再 `getSharedPreferences("dSIM_UI_PREFS")` 直读。
- **C3**：线上格式仅 DSM3；改盐/轮数/AAD = 改格式 → 必须换魔数。
- **C11**：必达消息（入站短信同步）必须走 `sync_outbox`，不能直接 publish。
- **C12**：持久会话（固定 clientId / cleanSession=false / 文件持久化）不能改。
- **C13**：任何 publish 都走 `CloudTopics.publishTopic`，不得发到裸 `<base>`。
- **C14**：PONG 只经 `publishDeviceSnapshot(force)`；`ONLINE_TIMEOUT_MS` 必须 > `MAX_SILENCE_MS`（有单测断言）。
- 兼容性：旧构建（DSM2/裸 topic）与当前构建**双向不通**；所有测试模拟器需重装。

**批次 E 新增/收紧：**
- **C11 扩展**（W21）：必达控制消息（`SEND_CMD_RESULT`、`HISTORY_SYNC_ACK`、已发短信的 silentSync 同步）走 `SyncOutbox.enqueueControl`。discriminator 区分同一 UUID 的不同结论；同一结论重放会合并。**刻意不进发件箱**：PING/PONG/OFFLINE（可丢）与用户发起的 `SEND_CMD`（后台补发违反「不自动重发」前提）。
- **C13 收紧**（W7）：`globalMqttClient` 私有。服务外唯一发布途径是 `MqttSyncService.publishToGroup(...)`（火忘）或 `SyncOutbox.enqueueControl(...)`（必达）。别再加 `companion` 公开客户端；别复活 `ACTION_PUBLISH_MSG` 之类的旁路。
- **协议改动**（W5）：新增/修改线上消息字段只改 `MqttProtocol.kt` 的 data class；`decode` 必须对未知 action 返回 null（有单测）。旧设备发的多余/缺失字段要靠默认值兜住。
- **错误处理**（W6）：不再允许 `catch (_: Exception) {}`。预期回退用 `Log.d` + 一句为什么可忽略；真实失败 `Log.w(tag, msg, e)`。`CancellationException` 必须先 rethrow（现有代码都这么写）。
- `manualDisconnectInCurrentSession` 保留为 companion 可变量——它必须跨 START_STICKY 重启存活。这是有意的，不是遗漏。

## 5. 待办（按 checkpoint 顺序）

**批次 C — ✅ 已完成（`f64188a`）**

**批次 D — ✅ 已完成（`842a32a`）**。遗留：`EncryptedSharedPreferences`（W14）仍未做——口令在 `dSIM_UI_PREFS` 里仍是明文，但备份/迁移已排除，外流的主要途径已堵住；做之前要先想清楚密钥丢失（恢复出厂、Keystore 失效）后的降级路径。F9 的另一半（消息时间戳/nonce 防重放）也仍开放。

**批次 E — ✅ 已完成（`0d8f675` … `d3c49ee`）**

**批次 E 期间新发现、尚未处理的缺陷（按对「收到的短信不丢」的影响排序）**

1. ~~开机自启崩溃~~ **✅ 已修（批次 F1，计划目录 `.planning/2026-09-18-boot-fgs-type`）**：`MqttSyncService` 前台类型 `dataSync` → `remoteMessaging`（Android 15+ 唯一既允许从 `BOOT_COMPLETED` 启动、又无 6 h 预算、语义又匹配「设备间转送短信」的类型），`startForeground` 收口到 `promoteToForeground()`，被拒时 `stopSelf` 不再杀进程。API 36 模拟器 `adb reboot` 验证：开机 ~30 s 内 `dSIM_Boot → subscribed → PONG`，`dumpsys` 显示 `isForeground=true types=0x200`，随后入站短信 `sent=1`。新增不变量 AGENTS C19。真机请再 reboot 一次确认。**测试坑**：`am force-stop` 后包处于 stopped 态，不会收到 `BOOT_COMPLETED`——验证前必须先正常拉起一次；`am broadcast BOOT_COMPLETED` 在此镜像被拒（protected），只能真 reboot。原始记录：`BootReceiver` 用 `ACTION_CONNECT` 拉起 `MqttSyncService`，API 34+ 抛 `ForegroundServiceStartNotAllowedException: FGS type dataSync not allowed to start from BOOT_COMPLETED`（模拟器 `adb reboot` 复现，两次进程都崩）。后果：手机重启后守护进程起不来，直到用户打开 app；期间收到的短信会落库进发件箱（不丢），但同步延迟到用户开 app。改法候选：开机用 WorkManager/`JobScheduler` 延迟调度而非直接 FGS；或 FGS 类型改 `specialUse`（要在清单写 `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` 说明）；`remoteMessaging` 语义上最贴切但仍受 BOOT 限制。**需要用户拍板 FGS 类型策略**。
2. ~~断网恢复后不重连~~ **✅ 已修（批次 F2，`.planning/2026-09-18-reconnect-policy`）**。根因两条：(a) Paho `automaticReconnect` 只在「连上过再断」时工作，首连失败永不重试；(b) Paho 后台重连期间我们的 FLUSH/INIT 再走 `close()`+新建同 clientId 客户端，两者互踢 → 「一次连接失败然后沉默」。改法：`setAutomaticReconnect(false)`，服务自己 `scheduleReconnect`（`ReconnectPolicy` 5 s 倍增封顶 5 min，单 job），`registerDefaultNetworkCallback.onAvailable` 立即重试，重建前 `setCallback(null)`+`disconnectForcibly`+`close(true)`。模拟器 A/B：两轮 `svc wifi/data disable` 60 s（期间收一条短信）→ `enable` 后 11–14 s 内 `subscribed` + `outbox sent=1`，无需 force-stop；断网冷启动 → 失败退避 10/20/40 s → 网络恢复 32 s 内连上。新增 AGENTS C20、`ReconnectPolicyTest`。原始记录：`svc data/wifi disable` 45 s 后 `enable`，Paho 打了一次 `连接失败` 便沉默，需 force-stop 重启才恢复。W6 那次测试是 Paho 自己察觉 socket 断了才走 automaticReconnect；这次像是「重连尝试失败后 Paho 放弃 / 或 `connectAndSubscribe` 抛出后没有下一次触发」。发件箱兜住了数据，但恢复时间取决于用户何时开 app。**建议先在真机复现再改**；可能的改法：`connectionLost` 后由服务自己用指数退避重试，不完全依赖 Paho。
3. ~~W4 步骤 2~~ ✅ 随 F2 完成（重连状态收口到服务；独立 `ConnectionManager` 类未抽，见 REFACTORING W4）。
4. ~~「发送费用防护」~~ **✅ 已做（批次 F3，`.planning/2026-09-18-send-cost-guard`）**：请求端多段短信先弹「将拆分为 N 条计费短信」确认；执行端设置页新增「允许其他设备通过本机发短信」（默认开，关闭时回 `SEND_CMD_RESULT success=false`）与「每日代发上限」（默认 50，0 = 不限，按 `send_commands` 当日 `partCount` 求和、`state != FAILED`，超限走既有 prepare-failed 路径回 FAILED）。无 schema 变更。模拟器探针：开关关 → 1.3 s 收到 FAILED「已关闭代发」、无 claim；上限 2/3 → 第 N+1 条 FAILED「今日代发已达上限」，前一条正常 SENT。新增 AGENTS C21、`SendCostPolicyTest`。
5. ~~F9 另一半（时间戳/nonce 防重放）~~ **✅ 已做（批次 G，`.planning/2026-09-19-replay-guard`，一步到位、无兼容期）**：`encode()` 自动盖 `ts`+`nonce`，发件箱在 flush 时 `stamp()` 重盖（离线 > 10 min 的行不会过期）；`ReplayGuard` 在解码后拒 MISSING / STALE（±10 min，OFFLINE 24 h）/ DUPLICATE（同发送者 nonce，LRU 4096）。探针：重放同一密文 → 0 PONG + `DUPLICATE`；20 min 旧 ts → `STALE`；无信封 → `MISSING`；新鲜 PING 照常；断网 75 s 期间收的短信恢复后送达且 `ts` 为 flush 时刻。新增 AGENTS C22、`ReplayGuardTest` 7 例。**升级注意：所有设备必须同时升到此版本，旧版消息会被新版全部丢弃。**
   F11 已读同步暂缓：本地没有任何写 `isRead` 的入口，先做协议是死代码。
6. ~~W14 `EncryptedSharedPreferences`~~ **✅ 已做（批次 H，`.planning/2026-09-19-encrypted-credentials`，未引入 security-crypto——它已弃用且拖 Tink）**：`CredentialVault`（Keystore `dsim_cred_v1` AES-256-GCM，不绑用户认证以便开机读取）密封口令到 `PASSWORD_ENC`；首次读取时把旧明文 `PASSWORD` 迁走；密封值打不开 → 清空凭据 + `CREDENTIALS_RESET_REASON=key_lost` → 设置页弹「云端凭据已重置，请重新填写」。模拟器：旧构建明文 → 新构建首次启动 `migrated` 并 8 s 内 `subscribed`；换成别的密钥的假 blob → 启动即清空三键；重填 → 再次密封并连上；`adb reboot` → 开机自启仍能解封并连上。新增 AGENTS C23、`CredentialCodecTest`。
7. **入站手动 PUBACK — ✅ 已做（批次 I，`.planning/2026-09-19-inbound-manual-ack`）**：接收端 Paho 自动 ack 早于 Room 插入的窗口会丢对端短信（模拟器 A/B：400 条突发 + force-stop，旧构建丢 43 条）。`setManualAcks(true)` + `InboundDispatcher` 在处理器返回后 `messageArrivedComplete`；取消不 ack 由 Broker 重投；`handleIncomingMessage` 补 `CancellationException` rethrow。新构建 400/400、20 条 `dup=true` 重投。新增 AGENTS C24、`InboundDispatcherTest` 7。**环境变化**：公共 EMQX 对 QoS1 突发静默丢包，突发类 E2E 改用本机 Docker Mosquitto（容器 `dsim-mosq`，模拟器 BROKER 已改为 `tcp://10.0.2.2:1883`，`dsim_setbroker.sh <url>` 可改回）。
8. 其他 P1（未排期）：W2 `deviceId` 自管、W3 历史队列状态机去文案依赖、W8 `mappingKey` 诊断、F11 已读同步（等有写 `isRead` 的入口再做）。

## 6. 环境与工具（踩过的坑）

- 通过 AgentDock MCP（`https://mcp.1110022.xyz/mcp`）操作用户 Windows 机器；`exec_command` 运行时是 **PowerShell**（无 `head`/`iconv` 等），同步调用需 `execution_mode:"sync"` + `timeout_ms`，实际上限 ~90 s。参数名是 `cmd`/`workdir`（写错会静默返回空）。请求要带浏览器 User-Agent，否则 Cloudflare 1010。
- **PowerShell 5 把无 BOM 的 `.ps1` 当 GBK 读**：辅助脚本里含中文正则会炸（「Too many )'s」）。脚本保持纯 ASCII，输出前 `[Console]::OutputEncoding=UTF8`。
- **批次 E 新增脚本**（`C:\Users\admin\AgentDock\`）：`dsim_launch.ps1`（WMI 脱离启动 gradle 的包装，避免内联 `Invoke-CimMethod` 的引号地狱）、`dsim_probe_w5.py`（互操作探针，PONG 窗口 5 s 在慢主机上太紧）、**`dsim_probe_w7.py ping|sms`**（各等最多 70 s，优先用这个）、`dsim_probe_w21.py hist|sendcmd <mappingKey>`（历史 ACK / SEND_CMD 往返）、`dsim_w6_offline.ps1`/`dsim_w21off.ps1`（断网脚本）、`c8check.py`、`commit_msg_e1…e6.txt`。
- **模拟器只有 2 GB 内存**：Gradle 全量构建后宿主内存紧张，模拟器 swap 抖动（load 28），表现为 app 启动 10 s 超时被杀、FGS 未及时 `startForeground` 崩溃、`am start` 报 timeout。不是代码问题；`adb reboot` 或等 Gradle 释放后再测。
- 装完新 APK 后需要 ~60–90 s 才 `subscribed`；提前跑探针会得到 0 PONG（不是回归）。判据：`logcat -d | Select-String 'subscribed dsim'` 或 `/proc/net/tcp6` 有 `:22B3` ESTABLISHED。
- `MainActivity` 不导出，`am start` 会 Permission Denial；用 `monkey -p com.example.dsim -c android.intent.category.LAUNCHER 1`（落到 `SmsListActivity`）。
- **会话结束会杀子进程**（Job Object）：Gradle / 模拟器 / E2E 必须用 WMI 脱离启动：
  `Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine='"C:\Program Files\Git\bin\bash.exe" "C:\Users\admin\AgentDock\dsim_gradle.sh" <tasks>'; CurrentDirectory='...\dSIM'}`
  然后轮询 `C:\Users\admin\AgentDock\dsim_gradle.log.state`（`RUNNING`/`DONE exit=N`）与 `dsim_gradle.log`。
- JDK：`C:\Program Files\Microsoft\jdk-21.0.7.6-hotspot`（脚本已设 `JAVA_HOME`）。
- 辅助脚本全部在**仓库外** `C:\Users\admin\AgentDock\`：`dsim_gradle.sh`、`dsim_emu.sh`、`dsim_e2e_b*.sh`、`dsim_lwt_probe.py`、`dsim_peer_probe.py`、`dsim_dbdump.py`、`dsim_chk*.sh`。
- 模拟器 **dSIM_B 仍在 emulator-5554 运行**（已装批次 D 的 debug 包，连 `dsim/test/9f3a2b` @ broker.emqx.io，SMS 角色在 dSIM；**其 broker 已被手工改成 `ssl://…:8883` 用于 TLS 验证**，这是测试手段不是应用自己的迁移）；不需要时 `adb -s emulator-5554 emu kill`。物理设备 `e6f587b0` 未授权，从未使用。
- **重装 APK 会重置运行时权限**：`RECEIVE_SMS` 未授予时系统在入队阶段就跳过广播（`dumpsys activity broadcasts` 可见 `skipped by policy at enqueue`），现象是「一条接收器日志都没有」。装完先 `pm grant` 八项权限再测。
- **`:app:assembleRelease` 的 `lintVital*` 会联网拉依赖**，网络受限时会挂死（本次挂了 15 分钟，jstack 停在 SSL socket read）。确认依赖已缓存时加 `-x lintVitalAnalyzeRelease -x lintVitalReportRelease -x lintVitalRelease`；跳过不等于 lint 通过。
- 编码约束（AGENTS C8）：所有源文件 LF、无 BOM、UTF-8；提交前用脚本检查，`git diff --check` 通过。
- 手动断开路径无法从 adb 触发（`Requires permission not exported`），仅代码审查覆盖。
- **改已装应用的 prefs**：`run-as <pkg> sh -c "sed -i …"` 会 Permission denied（run-as 继承不到外层重定向）。走 `run-as cat` 导出 → 宿主改 → `adb push /data/local/tmp/` → `run-as cp`，且必须先 `am force-stop`，否则进程退出会用内存旧值覆盖回去。
- **验证备份排除必须做 A/B**：`bmgr fullbackup` 在「无数据可备」与「传输层失败」下表现相近，单次结果不能下结论。需前置 `bmgr enable true` + `bmgr transport com.android.localtransport/.LocalTransport`；生产镜像 `adb root` 不可用，读不到 localtransport 落盘目录，以 `PFTBT` 日志为准。

## 7. planning-with-files 状态

`.planning/` 下四个目录：
- `2026-09-17-untitled-9566bb32`（旧，与本线无关）
- `2026-09-18-correctness-fixes`（第一批，已完成）
- `2026-09-18-durable-sync-outbox`（批次 A，已完成；**`findings.md` 是完整审查清单 F1–F17 + 文档矛盾，后续批次以它为源**）
- `2026-09-18-battery-kdf-topics`（批次 B，已完成）
- `2026-09-18-hardening-oneliners`（批次 C，已完成）
- `2026-09-18-security-hardening`（批次 D，已完成；**`progress.md` 含全部验证证据与 A/B 方法**）
- `2026-09-18-engineering-cleanup`（批次 E，已完成；`progress.md` 含 E1–E6 每步的验证数据与环境备注；**`.planning/.active_plan` 现指向它**）

AgentDock 任务：`tsk_1dd1ab5cfda65306`、`tsk_9511e97f4e78dc5c`、`tsk_197d09fd37292069`、`tsk_44d5435e0cfaa2f8`（批次 C）、`tsk_0f4840847164b5b3`（批次 D）已 complete；`tsk_6b31f4a02a0f461c`（批次 E）已 checkpoint 到 E6，**未标 complete，等审查**；旧任务 `tsk_ca39523214c20688` 仍 active（无关，未动）。

**接手第一步**：读本文件 §8 → `git log --oneline -3`（HEAD 应为 `d3c49ee`）+ `git status`（应只有两个未跟踪 .md）→ 等用户审查结论。若用户放行，把 `tsk_6b31f4a02a0f461c` 标 complete，再按 §5 顺序开新计划目录。

## 8. 审查指引（今天 17 个提交，建议按风险从高到低看）

`git log --oneline fdaf92d..HEAD` 列全部；`git diff fdaf92d..HEAD --stat` 共 105 文件 +10269/−4672（含大量文档与 `.gitattributes` 重新检出的行尾变化，代码实际净变化远小于此数）。

**高风险（改了线上语义，建议逐行看）**
1. `a78cf09` 批次 B：加密格式 DSM3、topic 结构、心跳节流、LWT。**与旧构建双向不通**，这是今天唯一不可回滚兼容的决定。看 `DsimCryptoUtils`、`CloudTopics`、`HeartbeatPolicy`。
2. `81a786b` 批次 A：Room v6→v7 迁移 + `sync_outbox`；`MqttSyncService` 持久会话参数。看 `DsimDatabase` 迁移、`SyncOutbox.flush` 的删行时机（PUBACK 后）。
3. `d9f4dde` 第一批：`SEND_CMD` at-most-once（`send_commands` 表、`SendCommandPolicy`）。产品前提「不重复发送」全靠这里。
4. `d3c49ee` W21：控制消息进发件箱。看 `SyncOutbox.controlKey` 的唯一性设计（同 UUID 不同结论 vs 同结论重放）、`publishOutcome` 用记录自带 `groupFingerprint` 而非当前配置。
5. `7c2d55d` W7：`publishToGroup` 的守卫条件（topic **和** password 都要匹配）；确认删除 `ACTION_PUBLISH_MSG` 无外部调用方（`git grep` 为 0，且它未在清单声明为 intent-filter）。

**中风险（结构性重构，看行为是否等价）**
6. `8ba382e` W4：三文件抽取。重点核对 `MqttInboundHandler.handleIncomingMessage` 的分支顺序与抽取前一致（架构说明书 §9.3 ①–⑯）。
7. `5fbcdca` W5：`MqttPayloadCodec.decode` 对旧格式的容忍；`ENCRYPTION_ERROR` 哨兵删除后所有 `encryptOrNull` 的 null 分支处理。
8. `842a32a` 批次 D：备份规则 deny-by-default、默认 broker 改 TLS（**存量配置不迁移**）、`validateBaseTopic` 的拒绝规则是否过严。

**低风险（可快速过）**
9. `62e133c` W6：只加日志 + `lastError` 字段 + 通知文案。
10. `0d8f675` 死代码；`f64188a` 批次 C；`f906653` 删 V1 兼容；`0f6b445` 测试夹具；`82c84f0` 与各 `docs(planning)` 提交。

**审查时可直接对照的证据**：每个批次的 `.planning/<dir>/progress.md` 里有 logcat 片段与探针输出；REFACTORING.md 每个 ✅ 条目下有「已做 / 未做 / 验证」三段。

**审查后若要回退某项**：所有提交都在本地，`git revert <sha>` 即可；只有 `a78cf09`（DSM3）回退会让其后所有模拟器数据/探针脚本失效，慎重。
