# 2026-09-18：正确性修复第一批

此文件覆盖旧文档中与下列改动冲突的描述。基线 `82c84f0`。

## 已实现

1. **命令副作用幂等**：新增 Room v6 `send_commands` 表。`INSERT OR IGNORE` 原子认领 UUID 与准备本地短信记录处于同一事务中；只有认领者可以进入 SmsManager。不能用短信表中已有 status=0 的记录判断是否已经执行。
2. **真实发送结果**：`SmsSentResultReceiver` 是非导出组件，经显式 immutable PendingIntent 接收系统 sent 回调。所有分段成功才为 SENT，有分段失败则最终 FAILED；重复、越界回调不会重复推进。成功仅指系统发送成功，不代表收件人已送达/阅读。
3. **未知状态**：新增短信 status=-2（UNKNOWN）。API 调用异常或查询时发现回调等待超过 120 秒，不盲目重发。迟到的有效回调可以解决 UNKNOWN。
4. **结果查询**：点击发送中/未知/失败状态会以原 UUID 查询/恢复结果。已认领的 UUID 永不重新执行；需要重新发送时须由用户明确新建短信，生成新 UUID。原来的“重试”按钮文案已调整，防止误解。
5. **恢复配置**：冷启动从保存的 Broker/Topic/口令恢复整组配置，再应用明确的 Intent 覆盖；null Intent 的 sticky 恢复尊重自动连接配置。
6. **回执校验**：拒绝空/非本机 targetDeviceId，并核对当前 SIM 配置中的执行设备。PENDING/UNKNOWN 不覆盖已经确认的最终状态。
7. **生命周期**：onDestroy 取消 Service scope；初次连接互斥。所有新结果发布检查模式和当前组配置，迟到回调不向切换后的其他同步组发布。
8. **数据库初始化**：同步锁内二次检查单例，避免并发创建多个 Room 实例。

## 数据迁移与保守恢复

- Room 从 v5 升到 v6，保留既有全部迁移与短信 UUID 唯一索引。
- 所有旧版出站短信生成 UNKNOWN 执行墓碑：旧实现无法证明它们是否已经调用过短信 API，禁止升级后重放；旧短信本身不被删除/改写。
- 删除会话短信不删除执行墓碑。暂不做自动清理，防止清理后旧密文重新变成可执行命令。
- 不承诺跨短信系统和数据库的 exactly-once。进程在认领后、调用前崩溃，可能留下实际未发送的 UNKNOWN；这是避免重复发送的保守取舍。
- 结果通知网络发送是 best-effort：先持久化结果并释放 BroadcastReceiver，再尝试发布。离线时可用相同 UUID 查询恢复，但尚无持久自动回执 outbox。
- 加密当时只读写 DSM2（第三批已升为 DSM3，见下）。不同版本设备应一起升级；旧端不能正确展示新增 UNKNOWN 状态。

## 第二批（同日，基线 d9f4dde）：入站短信可靠同步

产品前提（用户确认）：运营商发短信按条收费，产品价值是"收到的短信经互联网免费同步到其他设备"，默认有网络。因此本批只做入站同步链路，运营商发送侧（本机直发、默认短信组件）降级。

9. **发件箱**：Room v6 → v7 新增 `sync_outbox`。`SmsReceiver` 在同一事务内写短信 + 发件箱行，然后 `ACTION_FLUSH_OUTBOX` 唤起服务；Receiver 不再持有 MQTT 客户端引用。`SyncOutbox.flush` 单飞、按 createdAt 顺序、阻塞式 QoS1（等 PUBACK）后删行；首个失败即停，下次触发（连接建立 / 重连 / 心跳 / 显式）续传。组指纹不匹配的行直接丢弃，不会发到切换后的其他组。payload 明文存储、发布时才加密，因此改口令后仍能正确加密。
10. **持久会话**：clientId 固定为 `dSIM_<deviceId>`，`cleanSession=false`，`MqttDefaultFilePersistence(filesDir/mqtt)`。Broker 为离线设备排队 QoS1 消息。公共测试 Broker 的会话保留时长不受我们控制，已在文档注明。
11. **通知**：有积压时显示「已连接，N 条短信待同步」。
12. 用法模式闸门（C7）在入队时评估：`canUploadIncomingSms=false` 不入队；冲刷时再检查 `canUseCloud`。

验证：JVM 21/21（新增 `OutboxPolicyTest` 7）；emulator-5554 仪器测试 10/10（新增 `SyncOutboxDaoTest` 5，含 v6→v7 迁移；`SendCommandLedgerTest` 迁移用例改为 5→6→7 链式）。端到端：断网注入 2 条短信 → 落库、无发布尝试、通知显示连接失败 → 恢复网络后 `outbox[connect] sent=2 remaining=0`。

本批未改：`OutgoingSmsDispatcher.publishOutcome`、历史导入 ACK 循环、`publishEncryptedSms` 仍是直发（范围控制，见 `.planning/2026-09-18-durable-sync-outbox/task_plan.md` D6）。

## 第三批（同日，基线 c765de2）：耗电与 topic 布局

13. **加密 V3（`DSM3`）**：`[4B "DSM3"][12B IV][AES-256-GCM 密文+标签]`，AAD = 魔数。主密钥 = PBKDF2-HMAC-SHA256(口令, 固定盐 `dSIM/v3/master-key`, 120 000 轮)，**每个口令只派生一次**并缓存（LRU 上限 4）。此前每条报文各自随机盐、收发双方每条都跑 12 万轮。`encryptMessage/decryptMessage` 签名不变；DSM2 读取路径已删除（无已发布设备）。不同版本设备必须一起升级。
14. **每设备发布 topic**：发布到 `<base>/<deviceId>`，订阅 `<base>/+`（`CloudTopics`）。`messageArrived` 第一行按 topic 后缀丢弃自身回声，不解密。载荷内 `deviceId == 本机` 的报文仍在解密后二次丢弃（防伪造子 topic 重放）。`base` 本身不再有人发布；旧版设备发到 `base` 的报文新版本收不到。
15. **心跳按变化发布**：`HeartbeatPolicy` 定时器 30 秒一 tick；只在快照指纹（设备名、电量按 5% 分桶、充电、默认短信、SIM 摘要、队列状态/进度，不含 `updatedAt`）变化时发 PONG，或距上次发布 ≥120 秒兜底。收到 PING、连接/重连时强制发。`ONLINE_TIMEOUT_MS` 45 秒 → 5 分钟。
16. **OFFLINE 动作 + Last Will**：手动断开 / 切本地模式 / `onDestroy` 主动发 `{"action":"OFFLINE"}`；连接时注册同内容的 LWT，TCP 异常断开由 Broker 代发。对端 `DeviceDirectoryManager.markOffline` 把 `lastSeenAt` 推到在线窗口之外，界面立即显示离线，不等 5 分钟。
17. 收到 PONG 不再弹 Toast（原每 20 秒一次的 UI 噪音）。

验证：JVM 39/39（新增 `DsimCryptoUtilsTest` 8、`CloudTopicsTest` 4、`HeartbeatPolicyTest` 6）；`assembleDebug` 通过。emulator-5554 端到端：订阅 `dsim/test/9f3a2b/+`、发布 `dsim/test/9f3a2b/<deviceId>`；自身 PING/PONG/OFFLINE 全部 `skip own echo`，`dSIM_Crypto` 零告警；静置 4 分钟只有 keepalive PONG（间隔 120 s），拔电/换充电状态后下一 tick 内发 `reason=changed`；`am force-stop` 后外部订阅者收到 Broker 代发的 LWT OFFLINE（DSM3，可用 Python 交叉解密）；外部模拟对端发 PONG→OFFLINE，本机日志 `peer OFFLINE`，DB 中该设备 `lastSeenAt` 被推到窗口外；伪造子 topic 上带本机 deviceId 的 OFFLINE 被忽略。批次 A 的入站短信发件箱路径在新 topic 上 `sent=1 remaining=0`。

本批未改：`SEND_CMD_RESULT` / 历史 ACK 仍直发（W21）；用户输入的 base topic 未做 `+`/`#` 合法性校验（批次 D）；`SmsReceivedReceiver` 的 `BROADCAST_SMS` 权限、`.gitattributes`、W15（批次 C）。

## 第四批（同日，基线 0c86a5d）：一行级加固

18. **`SmsReceivedReceiver` 加 `BROADCAST_SMS`**：该接收器 `exported=true` 且带 `SMS_RECEIVED` 过滤器，此前没有权限限定，任何应用都能构造 PDU 广播，被本机落库、回写系统库并同步给全组设备。`SmsReceiver`（`SMS_DELIVER`）一直有这个限定，本次补齐另一半。C6 的动作互斥规则不变。
19. **`.gitattributes`**：默认 `* text=auto eol=lf`，`*.bat`/`*.cmd` 与 `gradlew.bat` 固定 `eol=crlf`（否则 cmd.exe 可能解析失败），图片/jar/字体等标 `binary`。随后 `git add --renormalize .` 并重新检出，17 个 CRLF 文件转为 LF；`gradlew.bat` 按设计保留 CRLF。此前全靠 `core.autocrlf=true` 兜底，换台机器就会产生整文件 diff（C8 的落实）。
20. **调试面板不进 release（W15）**：`<activity .MainActivity>` 从 `app/src/main/AndroidManifest.xml` 移到新增的 `app/src/debug/AndroidManifest.xml`，release 清单里不再有这个组件；`SettingsActivity` 的入口按钮用 `BuildConfig.DEBUG` 包裹并在非 debug 下 `GONE`。注意 AGP 8 默认不生成 `BuildConfig`，为此在 `app/build.gradle.kts` 打开 `buildFeatures { buildConfig = true }`。`MainActivity.kt` 仍参与 release 编译（`SettingsActivity` 引用它），但无清单声明即无法启动。

验证：JVM 39/39；`assembleDebug` 与 `assembleRelease` 均 PASS。`aapt2 dump xmltree`：release APK 的 `MainActivity` 引用数 0，debug APK 为 2；两个变体中 `SmsReceiver` 与 `SmsReceivedReceiver` 都带权限限定。emulator-5554 实测：把 SMS 角色临时让给 Google Messages → 真实入站短信走 `SMS_RECEIVED` 正常到达 `SmsReceivedReceiver`（`dSIM_Receiver: Captured incoming SMS action=…SMS_RECEIVED`，`outbox sent=1 remaining=0`），证明加权限没有阻断系统投递；角色还给 dSIM 后 `SMS_DELIVER` 路径回归正常；debug 包里「设置 → 测试功能」仍能进入 `MainActivity`。

注意：`adb shell am broadcast -a …SMS_RECEIVED` 一直会被系统按 protected broadcast 拒绝（`uid=2000`），**这条不能用来证明本次修复生效**；真正的证据是带 `RECEIVE_SMS` 的系统投递仍然成功，以及清单里权限限定存在。

本批未改：批次 D（backup 规则、prefs 收口、默认 `ssl://`、base topic 通配符校验）与批次 E 全部条目；W21 仍开放。

## 第五批（同日，基线 f64188a）：安全加固

21. **备份与迁移改为 deny-by-default（F8）**：`backup_rules.xml` 与 `data_extraction_rules.xml` 此前是 Android Studio 模板，规则全部处于注释中。备份规则是 **allow-by-default** 语义，于是 `dSIM_UI_PREFS.xml`（明文 `PASSWORD`）与整个 Room 库都可以进 Google Drive 备份和换机迁移。现在两个文件都排除全部 domain（`root`/`file`/`database`/`sharedpref`/`external`，data-extraction 另加四个 `device_*`），且 `<cloud-backup>` 与 `<device-transfer>` 分别写——后者**不受 `android:allowBackup` 约束**，必须单列。刻意不用「逐个文件点名排除」：那份清单会在下次改存储名时静默失效。
22. **云端凭据收口到 `CloudSettingsManager`（F12/F16）**：13 处 `getSharedPreferences("dSIM_UI_PREFS")` 直读改为走访问器。其中 9 处读 BROKER/TOPIC/PASSWORD → `CloudSettingsManager.getConfig()`；4 处读写 `IS_MUTED` → 新增 `NotificationPreferences`（静音是 UI 状态不是云端配置，不并进 CloudSettingsManager）。`MainActivity` 里直接 `putString("BROKER"/"TOPIC"/"PASSWORD")` 的写入口也改走 `saveConfig`。`USAGE_MODE_EXPANDED` 是纯 UI 折叠态、已在 `SettingsActivity` 私有常量后面，保持原样。收口后 `app/src` 下该模式的直读命中数为 0。
23. **默认 Broker 改 `ssl://broker.emqx.io:8883`（F9）**：载荷本来就是 DSM3 端到端加密，但 `tcp://` 会把房间号、收发时间和报文大小暴露给链路上任何人。已实测 `broker.emqx.io:8883` 提供**公网可信证书**（RapidSSL 签发 `*.emqx.io`，2027-01-16 到期），所以 Paho 默认的 `SSLSocketFactory` 用系统信任库即可校验通过，不需要挂 CA、不需要自定义 TrustManager。**已保存的配置不迁移**：`getConfig` 仍返回用户存的值，只有全新安装或清空输入框才拿到新默认值，存量 `tcp://` 用户不会被打断。设置页对明文/公共 broker 给出一行提示，只提示不拦截。
24. **房间号通配符校验（F9 后半）**：新增 `CloudSettingsManager.validateBaseTopic`，在 `SettingsActivity` / `OnboardingActivity` / 调试面板三个保存入口拒绝 `+`、`#`、空白、控制字符、前导 `/`、连续 `//`、超 200 字符，并把结果归一化（`trim` + 去尾部 `/`）。这是 C13 的前提：base topic 含通配符会让 `<base>/<deviceId>` 变成非法发布目标、`<base>/+` 变成过宽订阅。校验放在保存入口而不是 `CloudTopics`，因为老用户 prefs 里已存在的 topic 仍要能读出来用。

验证：JVM 52/52（新增 `CloudSettingsManagerTest` 13 例，覆盖校验表、broker 分类、提示文案完备性）；`assembleDebug` PASS。emulator-5554 实测：
- **TLS 真连**：把已存配置改成 `ssl://broker.emqx.io:8883` 后重启应用，`/proc/net/tcp6` 出现 uid 10227 到 `:22B3`(=8883) 的 ESTABLISHED 连接，日志 `subscribed dsim/test/9f3a2b/+, publishing on .../d9e0118b7d57f7c6`，通知栏「已连接」。随后 `emu sms send 10086` 真实入站短信 → `dSIM_Receiver: Captured incoming SMS action=…SMS_DELIVER` → `dSIM_Outbox: flush sent=1 dropped=0 failed=0 remaining=0`，证明 TLS 下同步链路完好。
- **备份排除做了 A/B 对照**：deny 规则下 `bmgr fullbackup com.example.dsim` → `PFTBT: Error -1002 … Transport rejected backup`（无数据可备份）；临时换回空的宽松规则重新构建安装后，同一命令 → `Full backup completed with status: 0` 且无 -1002，证明拒绝来自新规则而非环境问题。对照结束后已还原并重新构建安装。
- `aapt2 dump xmltree` 确认两份规则以 5 条 / 18 条 `<exclude>` 正确编入 APK。

本批未改：批次 E 全部条目（死代码清理、协议数据类化、拆 `MqttSyncService`、静默 catch 收口、W21 回执走 outbox）；`EncryptedSharedPreferences`（W14）仍未做，但备份排除已挡住口令外流的主要途径。

## 第九批（2026-09-19，基线 2e5573f）：入站手动 PUBACK

产品前提回顾：核心是「收到的短信不丢地同步到其他设备」。批次 A 保证了**发送端**不丢（发件箱 + PUBACK 后删行），但**接收端**仍有一个窗口：Paho 在 `messageArrived` 返回时自动 PUBACK，而我们在回调里只是 `serviceScope.launch { handleIncomingMessage }` 就返回，Room 插入在之后才发生。进程在这个窗口被杀（LMK、用户 force-stop、`onDestroy` 取消 scope），Broker 已认为送达，持久会话（C12）也不会重投——这条短信在接收设备上永久丢失。

18. **`InboundDispatcher` + 手动 ack**：`client.setManualAcks(true)`；`messageArrived` 只把 (topic, payload, id, qos, dup) 交给 `InboundDispatcher`。自身回声按 topic 立即 ack、不解密；其余消息在 `handleIncomingMessage` **返回后**调 `messageArrivedComplete(id, qos)`。处理器被取消 → 不 ack、rethrow；处理器抛其他异常 → 记日志后 ack（毒消息不能堵住 Broker 的在途窗口）。`dup=true` 的重投打 debug 日志。
19. **`handleIncomingMessage` 吞 `CancellationException` 修复**：原来的 `catch (e: Exception)` 把取消当成正常返回，18 的「取消不 ack」在这里会被抵消；现在先 `catch (CancellationException) { throw e }`。
20. 幂等性不变：重投的 SmsSync 靠 `sms_messages.uuid` 唯一索引 + `checkUuidExists`；重投的 SEND_CMD 靠 `send_commands` 原子认领；`ReplayGuard` 是进程内状态，新进程从空表开始，所以重投的 nonce 会被接受（这是期望行为，不是漏洞：同一密文只能被 Broker 重投一次，且只投给同一 clientId）。

无 schema / 协议变化；ack 时机是本地行为，新旧构建可互通。

验证：JVM 94/94（新增 `InboundDispatcherTest` 7：回声不跑处理器即 ack、ack 严格晚于处理器返回、取消永不 ack、scope 取消留在途、处理器异常仍 ack、ack 抛错不外泄、非本组 topic 的 sender 为 null）；`assembleDebug` + `assembleRelease`（跳过 lintVital）通过。模拟器 A/B（本机 Docker Mosquitto 2.1，逐包日志可查）：探针 QoS1 突发 400 条唯一 SmsSync → +1 s `am force-stop` → 重新拉起：
- **旧构建（2e5573f）**：Broker 收到 PUBACK 505 条 / 发出 525 条，落库 357/400，**丢 43 条**（idx 0、4–45，即 ack 已发出但尚未插入的那一段）。
- **新构建**：落库 **400/400**，Broker 日志显示重连（`session taken over`）后 `d1, q1` 重投 20 条，app 日志 20 条 `dup=true`，0 条被 ReplayGuard 拒绝。
- 冒烟：PING→PONG 0.5 s；重放 PING → `DUPLICATE`、无多余 PONG；`emu sms send` → `outbox[capture] sent=1`；300 条持续负载（无 kill，超过 Mosquitto `max_inflight=20`）全部落库 62 s，ack 节奏与 Broker 发包节奏一致、无堵塞。

**测试环境说明**：公共 `broker.emqx.io` 对同一发布者 QoS1 突发会**静默丢弃**约 25–75%（40 条只有 10 条到达独立见证订阅者，200 条到 70 条；与 dSIM 无关，Mosquitto 本机 200/200）。突发类 E2E 必须用本机 Broker（`docker run eclipse-mosquitto:2`，模拟器侧 `tcp://10.0.2.2:1883`）；模拟器 dSIM_B 的 BROKER 已被改为本机地址。

## 未完成（不要标为已修复）

- 本机卡脱离 MQTT 直接发送；默认短信应用的撰写/静默回复/彩信能力。（已降级）
- ~~Android 15+ dataSync 前台服务时间限制、开机启动策略与服务类型设计。~~ 已在批次 F1 处理：`MqttSyncService` 改为 `remoteMessaging`（AGENTS C19）。
- 发送回执 / 历史 ACK 尚未走发件箱；历史 ACK 有效接收者和确认范围。
- 凭据仍以明文存于 `dSIM_UI_PREFS`（备份/迁移已排除，但本机 root 或 adb backup 之外的取证仍可读）；完整连接状态机/全局客户端并发治理。
- 无回调时的自动超时扫描；当前在用户查询时识别超时，不是后台自动扫描。
- 真实运营商网络上的单卡/双卡、长短信与失败回调测试。

## 文档修正

- 默认短信组件占位说明业务缺失，不代表系统必然拒绝角色；finish() 不是崩溃。
- prefs 随机 UUID 只能作为安装级标识，不能无条件保证卸载重装或换签名后保持不变。
- verify-dsim.sh 的 12 项是烟雾检查，不等于真实运营商发送、重复指令和全设备历史落库均通过。

## 验证

执行结果写入 `.planning/2026-09-18-correctness-fixes/progress.md` 与 `checkpoint.md`。
新增 `SendCommandPolicyTest`、`CloudConfigRestoreTest`、`SendCommandLedgerTest`。
设备测试只用独立临时数据库，不调用 SmsManager、不发布 MQTT、不操作实体手机。
