# 裁决与下一步（2026-09-20）

- 作出方：审查方（用户授权"遇到选择直接帮你选"）
- 基线：`main` 已推送至 `origin/main`；本文件与推送同批
- 依据：`修复计划_2026-09-19.md`、`代码质量审查/` 下 6 份审查（阶段 0 执行/收尾、阶段 1 执行/收尾、F6-F8 复核、阶段 0 执行）、`FIXES_2026-09-19.md`

## 1. 已决事项（本轮一次性清空待裁决队列）

| # | 事项 | 裁决 | 理由 |
|---|---|---|---|
| 1 | **push（ahead 25）** | **立即推送** | 阶段 1 产品代码已通过审查、F-1~F-10 全部闭环；长期只留单机副本是纯风险。推送前已做密钥扫描：无口令/令牌字面量，测试口令始终走 `DSIM_TEST_PASSWORD` 环境变量 |
| 2 | **D1–D5**（阶段 1 六项偏差中的五项） | **接受** | 逐项见 `代码质量审查/阶段1执行审查_2026-09-20.md` §4：D1 语义正确、D2/D3 有 LWT 兜底、D4 惰性创建合理、D5 属诊断信息降级 |
| 3 | **D6**（`sealOverride` 测试接缝） | 已由 F-2 收口（`check(BuildConfig.DEBUG)`） | 无需再动 |
| 4 | **W23**（旧身份残留一次性清理） | **不实现，保持登记** | 项目无发布用户；`pm clear` 重装即可规避；无可复用收益 |
| 5 | **F4**（过期 SEND_CMD 回执） | **批准，但并入阶段 2 一起做** | 与 T2.x 同属发送指令生命周期，避免二次改动同一段 ack/时序逻辑 |
| 6 | **F2 有界隔离/告警** | **暂不实现** | 取舍已写入 AGENTS/FIXES；阶段 2 若触及 outbox/ack 时序再评估 |
| 7 | **UTP 控制台 "21/20"** | **结案：报表伪影** | 用例级三方一致：XML `tests=20 failures=0 errors=0 skipped=1`、textproto 的 `test_case` 块数 **20**（19 PASSED + 1 IGNORED）；`test_status: PASSED` 的 20 行含一条聚合重复计数。判定标准固定为**用例级 XML/textproto**，不追 runner |
| 8 | **lint 5 个存量 error** | 不修 | 属阶段 4 的 CI 门禁议题（T4.3），不为绿灯改产品代码或基线 |
| 9 | **5558 新 AVD** | 保留供复验 | 已含 1 条合成短信，今后不得再称"空库" |
| 10 | 审查文档（11 份）与 3 个 `.planning` 脏项 | 继续**不入库** | 审查过程与产品历史分离；根目录垃圾清理归阶段 6 |

## 2. 批次划分（执行方按序推进）

### 批次 A（现在开始）
1. **阶段 3**：T3.1 删 `getRecentConversationsFlow` · T3.2 删 `hasKnownMagic` · T3.3 BootReceiver 两处 · T3.4 入站短信内容级判重 · T3.5 注释漂移 · T3.6 占位组件显式日志。逐任务独立 commit，每项给真实命令与输出。
2. **API 24/25（或 28）运行时验证**（与阶段 3 同批，价值最高的一项）：
   - 新建低版本 AVD（现有 AVD 最低 API 36）；
   - 在该设备上完成 **T0.1（Base64/nonce 编码）** 与 **F1（`HardwareProbeUtils` 的 `getSimState`/`getSubscriptionIds` 守卫）** 两条路径的实测——这两个 P0 级修复至今只有 lint 背书，是本项目最大的一处证据缺口；
   - 报告要给出真实命令与输出；做不到就如实写"未完成"及原因，不得用 lint 或 JVM 绿灯替代。

### 批次 B（批次 A 完成后）
**阶段 2 + F4**：T2.1 额度预留与认领同事务 + 分段计账 → T2.2 结果与两条 outbox 行同事务 → T2.3 本机卡发送入口收口 → F4 过期 SEND_CMD 回执（前置判断见 `FIXES_2026-09-19.md` 的 F4 节，尤其"先查 `getSendCommand(uuid) == null`"）。**每个任务做完停点汇报**，这是首次改动费用与发送状态的代码。

### 批次 C（按需）
同进程断网/重连 E2E（脚本早已备好）· release 包上的 T1.4/T1.5 界面与 logcat 复核。

## 3. 仍不批准

- 阶段 4/5/6 的开工（发布工程、持续重构、工程化收尾）——待批次 A/B 完成后按剩余精力决定。
- 任何为通过 lint 存量 error 而修改产品代码或加 lint 基线的做法。

## 4. 验收口径（沿用本轮新增约定）

- 每轮回归必须含 `:app:connectedDebugAndroidTest`，且该轮至少一次在**全新安装或清数据**设备上执行；
- 通过标准是**用例级**：任务正常完成、预期用例齐全、`failures=0` 且 `errors=0`；已解释的 fixture `skipped` 可接受，但**不代表该断言已验证**；
- 权限类用例的绿灯只在该轮设备为新装/清数据时才可信（`GrantPermissionRule` 不自动撤销）。
